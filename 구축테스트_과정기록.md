# 구축 테스트 과정 기록

> Vagrant + Rocky Linux 9 + k3s 로컬 클러스터에 Reverdi 애플리케이션 배포
> **AWS EKS 이관 전 검증**
> 갱신 2026-09-03 — 저장소명 변경(`reluxe` → `reverdi`) 재구축 포함

---

## 1. 구성

```
호스트 (Windows · RAM 64GB)
 └─ VirtualBox + Vagrant
     ├─ node0  192.168.56.10   컨트롤 플레인 전용     4GB
     ├─ node1  192.168.56.11   웹 + DB               4GB
     ├─ node2  192.168.56.12   웹 + DB               4GB
     ├─ node3  192.168.56.13   웹 + DB               4GB
     ├─ node4  192.168.56.14   배치 · 이미지 빌드     8GB
     └─ node5  192.168.56.15   레지스트리 · 인프라   16GB
                                    합계 RAM 40GB · 디스크 280GB
```

**Rocky Linux 9 를 고른 이유** — EKS 워커 노드(Amazon Linux 2023)와 같은 RHEL 계열입니다.
`dnf` · firewalld · SELinux enforcing 이 그대로라 여기서 익힌 것이 AWS 에서 통합니다.

### AWS 구성과의 대응

| AWS | 로컬 |
|---|---|
| EKS 컨트롤 플레인 (VPC 밖) | node0 — taint 로 격리 |
| `reverdi-web` 노드그룹 | node1~3 (`workload=web`) |
| `reverdi-batch` 노드그룹 | node4 (`workload=batch` + taint) |
| 운영·관측 EC2 | node5 (`workload=infra` + taint) |
| RDS Multi-AZ + 읽기 복제본 | CloudNativePG (primary + replica 2) |
| ECR | Docker Registry (`registry:2`) |
| ALB Ingress | NodePort 30080 |

---

## 2. 🔴 재현 순서

처음 문서에는 Secret 을 먼저 만들라고 적었는데 **순서가 틀렸습니다.** (문제 11)

```
①  VM 6대 생성                  vagrant up
②  k3s 서버 (node0)             🔴 --flannel-iface 필수
③  k3s 에이전트 (node1~5)       🔴 --flannel-iface 필수
④  taint · 네임스페이스
⑤  레지스트리 + registries.yaml 전 노드 배포
⑥  이미지 빌드 · 푸시 (node4)
⑦  🔴 DB 클러스터 생성
⑧  🔴 healthy 될 때까지 대기
⑨  🔴 CNPG 가 만든 비밀번호를 꺼낸다
⑩  그 값으로 앱 Secret 생성
⑪  helm upgrade --install
```

**⑦~⑩ 을 바꾸면 반드시 실패합니다.** CNPG 가 초기화 시점에 Secret 을 새로 만들어 덮어씁니다.

---

## 3. 겪은 문제 32건

### 우리 코드의 실수 (3건) — 고쳤습니다

| # | 증상 | 원인 | 고친 파일 |
|:--:|---|---|---|
| 1 | 웹훅 타임아웃 · 노드 간 파드 통신 불가 | **Flannel 이 Vagrant NAT 인터페이스를 잡음** | `k3s-server.sh` · `k3s-agent.sh` |
| 2 | `configmap not found` | Helm 훅이 일반 리소스보다 먼저 실행 | `migrate-job.yaml` |
| 3 | 배포 후 파드가 한 노드에 몰림 | `topologySpread` 가 **옛 세대 파드까지 계산** | `deployment.yaml` |

### 환경 문제 (5건)

| # | 증상 | 원인 | 대응 |
|:--:|---|---|---|
| 4 | 박스 다운로드 404 | Rocky 9.6 이 Vault 로 이전 | `bento/rockylinux-9` |
| 5 | VM 부팅 타임아웃 | 디스크 리사이즈 + `cpuexecutioncap` | `boot_timeout 900` · cap 제거 |
| 6 | `set: pipefail\r` | PowerShell CRLF | `sed -i 's/\r$//'` |
| 7 | `tee: No such file` | 에이전트에 `/etc/rancher/k3s` 없음 | `mkdir -p` |
| 8 | `sudo k3s: command not found` | RHEL `sudo` 가 `/usr/local/bin` 제외 | 전체 경로 또는 kubeconfig |

### 이름 변경 중 새로 나온 것 (3건)

| # | 증상 | 원인 | 대응 |
|:--:|---|---|---|
| 9 | `control characters are not allowed` | **BOM** (PowerShell 자동 삽입) | BOM 제거 |
| 10 | BOM 제거 후에도 같은 오류 | **U+0080** (인코딩 변환 중 주석 손상) | 제어문자 일괄 제거 |
| 11 | `password authentication failed` | 🔴 **CNPG 가 Secret 을 덮어씀** | 순서 변경 (2장) |

---

## 4. 🔴 문제 1 — 가장 오래 걸린 것

### 증상

```
Error from server (InternalError): failed calling webhook "mcluster.cnpg.io":
context deadline exceeded
```

### 진단 — 하나씩 배제

| 확인 | 결과 |
|---|---|
| 노드 상태 | 6대 `Ready` ✅ |
| 오퍼레이터 파드 | `1/1 Running` ✅ |
| firewalld trusted 존 | `10.42/10.43` 등록됨 ✅ |
| firewalld 포트 | 6대 전부 `8472/udp` ✅ |
| **파드 직접 접속** | `curl https://10.42.4.2:9443/readyz` → **`exit=28`** 🔴 |

방화벽도 정상, 라우팅도 정상, 파드도 정상인데 **패킷만 안 갑니다.**

```bash
$ ip -d link show flannel.1
vxlan id 1 local 10.0.2.15 dev enp0s3      # ← 원인 발견
```

### 원인

Vagrant VM 에는 NIC 이 두 개입니다.

| 인터페이스 | 주소 | 특징 |
|---|---|---|
| `enp0s3` | `10.0.2.15` | Vagrant NAT — **6대 전부 동일** |
| `enp0s8` | `192.168.56.x` | 사설망 — 노드별 고유 |

**Flannel 이 NAT 쪽을 잡았습니다.** 모든 노드가 같은 IP 로 VXLAN 터널을 맺으려 하니 통신이 안 됩니다.

`--node-ip` 를 줬는데도 이렇게 된 이유 — 그건 **"쿠버네티스가 보는 노드 주소"만** 바꿉니다.
CNI 가 쓰는 인터페이스는 별개라 `--flannel-iface` 를 따로 줘야 합니다.

### 해결

```bash
--flannel-iface=enp0s8 --node-ip=192.168.56.1x
```

```
vxlan id 1 local 192.168.56.10 dev enp0s8      # 바뀜
Received Subnet Event with VxLan: PublicIP: 192.168.56.10
Received Subnet Event with VxLan: PublicIP: 192.168.56.11 ...
curl -k https://10.42.4.2:9443/readyz  →  OK  exit=0
```

### 배운 것

- **CNI 는 노드 IP 와 별개로 자기 인터페이스를 고릅니다.** NIC 이 여러 개인 환경의 흔한 함정입니다
- 증상이 표면적 원인(방화벽)을 가리켜 한 단계 더 파고들어야 했습니다
- 진단 순서: 노드 상태 → 방화벽 → **CNI 인터페이스**
- AWS EKS 는 VPC CNI 라 이 문제가 없지만, **"노드 간 통신이 안 될 때 어디를 봐야 하는지"** 는 그대로 쓰입니다

---

## 5. 🔴 문제 3 — 설정은 맞는데 동작이 안 된 사례

```
reverdi-web-...-6zbgh   node1
reverdi-web-...-9kljz   node1     ← 2개가 몰림
reverdi-web-...-rbf77   node2
                        node3 은 비어 있음
```

`topologySpreadConstraints` 를 넣고 `DoNotSchedule` 로 바꿔도 계속 몰렸습니다.

**원인** — `labelSelector` 가 롤링 업데이트 중 **옛 파드까지 셉니다.**

```
스케줄 시점:  node3 에 옛 파드 있음 → "여긴 찼다" → node1 로
그 직후:      옛 파드 종료 → node3 가 비었지만 이미 늦음
```

**규칙 위반이 아니었습니다.** 옛 파드까지 세면 `maxSkew: 1` 이 지켜집니다.
**세는 대상이 잘못된 것**이었습니다.

```yaml
matchLabelKeys:
  - pod-template-hash      # 같은 세대끼리만 계산 (k8s 1.27+)
```

`matchLabelKeys` 는 **이 문제 때문에 k8s 에 추가된 기능**입니다.

---

## 6. 🔴 문제 11 — CNPG Secret 덮어쓰기

### 증상

```
Error: failed pre-install: job reverdi-migrate-1 failed: BackoffLimitExceeded
```

Helm 훅 Job 은 로그를 잡기 어려워, **같은 이미지로 직접 돌렸습니다.**

```bash
kubectl run dbtest -n reverdi --rm -it --restart=Never \
  --image=<레지스트리>/reverdi-backend:dev \
  --env="DATABASE_URL=postgresql+asyncpg://reverdi:<PW>@reverdi-db-rw.reverdi.svc:5432/reverdi" \
  -- alembic upgrade head
```

```
asyncpg.exceptions.InvalidPasswordError: password authentication failed for user "reverdi"
```

### 원인

| | |
|---|---|
| 우리가 만든 Secret | `1cd8ba9d...` |
| CNPG 가 만든 것 | `98f0ea7d...` |

`bootstrap.initdb.secret.name` 을 명시하면 "기존 것을 쓴다"는 뜻일 줄 알았는데,
**CNPG 가 초기화 시점에 새로 만들어 덮어씁니다.**

### 해결

```bash
# ① DB 먼저
kubectl apply -f infra/postgres-cluster.yaml
# ② healthy 대기
kubectl get cluster -n reverdi
# ③ CNPG 가 만든 값을 꺼낸다
PW=$(kubectl get secret reverdi-db-app -n reverdi -o jsonpath='{.data.password}' | base64 -d)
# ④ 그 값으로 앱 Secret
kubectl create secret generic reverdi-secret -n reverdi \
  --from-literal=DATABASE_URL="postgresql+asyncpg://reverdi:${PW}@reverdi-db-rw.reverdi.svc:5432/reverdi" ...
```

### 배운 것

- **처음엔 우연히 순서가 맞아 안 걸렸습니다.** 재구축할 때 드러났습니다
- 오퍼레이터가 관리하는 리소스는 **우리가 만든 것을 덮어쓸 수 있습니다**
- Helm 훅 실패는 **같은 이미지로 직접 돌려보는 것**이 가장 빠른 진단입니다

---

## 7. 🔴 문제 9·10 — 인코딩

### BOM

```
error converting YAML to JSON: yaml: control characters are not allowed
```

PowerShell 로 파일을 고치면 **UTF-8 BOM(`EF BB BF`)이 자동으로 붙습니다.**

```bash
file 파일      # "(with BOM)" 표시
sed -i '1s/^\xEF\xBB\xBF//' 파일
```

**근본 대책**
```powershell
[System.IO.File]::WriteAllText($path, $content, (New-Object System.Text.UTF8Encoding $false))
```

### U+0080

BOM 을 지웠는데도 같은 오류가 났습니다.

| 검사 | 결과 |
|---|---|
| `grep -P '[\x00-\x1F]'` | 안 나옴 |
| `file` | "UTF-8 text" 정상 |
| `d.decode('utf-8')` | 성공 |
| **파이썬 YAML 파서** | 🔴 `unacceptable character #x0080` |

**`0x80~0x9F`(C1 제어문자)는 유효한 UTF-8 이지만 YAML 에서는 금지**입니다.
인코딩 변환 중 한글 주석 일부가 이 범위로 깨진 것이었습니다.

**배운 것** — `file` 명령과 UTF-8 디코딩 성공은 **YAML 유효성을 보장하지 않습니다.**

---

## 8. 검증 결과

| 항목 | 결과 |
|---|---|
| 노드 6대 | `Ready` · IP 각자 다름 |
| taint 격리 | node0 컨트롤 · node4 batch · node5 infra |
| **웹 파드 분산** | ✅ node1·2·3 에 **하나씩** |
| **DB 파드 분산** | ✅ node1·2·3 에 **하나씩** |
| **DB 페일오버** | ✅ 자동 승격 · **웹 응답 200 연속 유지** |
| 마이그레이션 순서 | ✅ Helm 훅으로 앱보다 먼저 (14단계 적용) |
| 읽기/쓰기 분리 | ✅ 로그에 `-ro` 연결 확인 |
| `/metrics` | ✅ Prometheus 지표 노출 |
| **크롤러** | ✅ node4 에서 Playwright + Chromium 동작 |
| **`/dev/shm`** | ✅ `tmpfs 1.0G` 마운트 |
| 중복 수집 방지 | ✅ "다른 프로세스가 수집 중" |
| 무중단 롤링 업데이트 | ✅ 새 파드 Ready 후 옛 파드 제거 |

### 페일오버 시연 (발표 영상 B)

```
09:37:40  200
09:37:41  200        ← primary 삭제
   ...
09:38:08  200        끊김 0회
```

`PRIMARY` 자동 승격 · 웹 파드 `RESTARTS 0`

### 애플리케이션 상태

```json
{
  "ready": true,
  "database":       { "connected": true },
  "database_write": { "connected": true },
  "migration":      { "up_to_date": true }
}
```

---

## 9. 남은 테스트

### 새 네임스페이스에서 재확인

| 항목 |
|---|
| 크롤러 CronJob |
| DB 페일오버 |

### 이번에 새로 가능해진 것

| 항목 | 방법 |
|---|---|
| **클릭 집계** | 크롤링 후 `POST /api/events/click` → 202 |
| **30분 버킷 유니크** | 같은 세션 2번 → **1건만** 쌓이는지 |
| **`DoNotSchedule` 동작** | 노드 하나 내려서 `Pending` 확인 |
| **`APP_ENV` 기동 가드** | `--set config.APP_ENV=production` → CrashLoopBackOff |

### 아직 안 한 것

| 항목 | 담당 |
|---|---|
| Jenkins · Argo CD · MinIO | C |
| pgdump · aggregate CronJob | B (진입점 미구현) |

---

## 10. AWS 로 이어지는 것

### 그대로 가는 것

- Helm 차트 `templates/` 12개 전부
- taint · toleration · nodeSelector
- `topologySpreadConstraints` + **`matchLabelKeys`** (키만 호스트명 → AZ)
- probe · PDB · rollingUpdate
- 읽기/쓰기 분리 (`-rw`/`-ro` → RDS writer/reader)

### 버려지는 것

| 로컬 | AWS |
|---|---|
| Docker Registry | ECR |
| CloudNativePG | RDS Multi-AZ |
| MinIO | S3 |
| NodePort | ALB Ingress |

**매니페스트는 버려지지만, 그것으로 검증한 앱 설정과 운영 감각은 남습니다.**

### 환경 전환

```bash
helm upgrade --install reverdi charts/reverdi -f values-vagrant.yaml   # 로컬
helm upgrade --install reverdi charts/reverdi -f values-aws.yaml       # AWS
```

> ⚠️ AWS 배포 전 `values-aws.yaml` 의 `<S3_BUCKET_NAME>` 을 채워야 합니다.
> `APP_ENV=production` 이라 비어 있으면 **기동을 거부**합니다. (의도된 동작)

---

## 11. 정리

**① 문서에 적어둔 함정이 실제로 나타났습니다**

Rocky 박스 404, 공유 폴더, firewalld — 사전 조사한 항목이 그대로 발생했습니다.

**② 예상하지 못한 문제가 더 어려웠습니다**

Flannel 인터페이스, `matchLabelKeys`, CNPG Secret 덮어쓰기 — 셋 다 문서에 없던 것이고
가장 오래 걸렸습니다. 공통점은 **증상이 표면적 원인을 가리킨다**는 것입니다.

**③ "돌아가게 하는 것"과 "제대로 고치는 것"은 다릅니다**

ConfigMap 문제를 훅으로 우회하면 당장은 돌아가지만 `helm rollback` 이 깨집니다.
부작용을 확인하고 다른 방법(`optional: true`)을 택했습니다.

**④ 재구축이 검증입니다**

이름을 바꾸며 처음부터 다시 세우자 **CNPG Secret 문제가 드러났습니다.**
한 번 성공한 것은 우연일 수 있고, **두 번째에 진짜가 보입니다.**

**⑤ 로컬 검증의 값어치**

32개 문제를 AWS 에서 만났다면 클러스터 재생성에 20분씩 걸리고 비용도 나갔을 것입니다.
로컬에서는 `vagrant destroy && vagrant up` 으로 5분이면 처음부터 다시 할 수 있었습니다.
