# 구축 테스트 문제 해결 기록

> Vagrant + k3s 로컬 클러스터 구축 과정에서 겪은 **32건**의 문제와 해결 과정
> 1차 구축(reluxe) · 2차 재구축(reverdi) · CI/CD 도입 전 과정

---

## 0. 한눈에

| 분류 | 건수 | 내용 |
|---|:--:|---|
| **환경 · 도구 문제** | 7 | 박스 404 · 부팅 타임아웃 · CRLF · 디렉터리 · PATH · BOM · 제어문자 |
| **우리 코드 · 설정의 실수** | **21** | Flannel · Helm 훅 · topologySpread · Jenkins agent · Argo CD toleration · root PATH · node-exporter effect · 서브차트 키 · 비밀값 범위 · **AWS 이관 4건** |
| **이해가 부족했던 것** | 4 | CNPG Secret ×2 · git 충돌 마커 · Jenkinsfile 미반영 |

### 가장 오래 걸린 3건

| # | 문제 | 왜 어려웠나 |
|:--:|---|---|
| **6** | 노드 간 파드 통신 불가 | 증상이 **표면적 원인(방화벽)을 가리킴** |
| **9** | 파드가 한 노드에 몰림 | **설정은 맞는데** 동작이 안 됨 |
| **12** | DB 인증 실패 | **1차에서는 우연히 안 걸림** |

> **19번(서브차트 키)** 도 같은 성격입니다.
> `kubectl patch` 로 당장 되게 만들어서 **값 파일이 안 먹는다는 걸 몰랐습니다.**
> 팀원이 재구축하다 발견했습니다.

---

## 1. Vagrant 박스 다운로드 404

### 증상
```
Adding box 'rockylinux/9' (v6.0.0) for provider: virtualbox
An error occurred while downloading the remote file.
The requested URL returned error: 404
```

### 원인
Rocky Linux 9.6 이 Vault 로 이전되었는데, Vagrant 레지스트리는 여전히 옛 `/pub` 경로를 가리킵니다.

**2024년부터 반복 신고된 문제**로, 2026년 7월에도 같은 신고가 올라와 있습니다.

### 해결
```ruby
config.vm.box = "bento/rockylinux-9"    # Chef 관리, 링크 안정적
```

### 배운 것
공개 이미지 저장소의 링크는 깨질 수 있습니다. **대안을 미리 문서에 적어두는 것**이 실무적입니다.

---

## 2. VM 부팅 타임아웃

### 증상
```
Timed out while waiting for the machine to boot.
```
node5 에서만 발생.

### 진단
```powershell
vagrant status
```
**6대 전부 `running`** — 부팅은 됐고 Vagrant 만 기다리다 포기한 것이었습니다.

### 원인
- node5 는 RAM 16GB + 디스크 100GB 리사이즈가 겹쳐 첫 부팅이 오래 걸림
- 기본 `boot_timeout` 300초로는 부족
- 호스트를 배려하려 넣은 **`cpuexecutioncap 90` 이 오히려 `dnf install` 을 느리게** 만듦

### 해결
```ruby
config.vm.boot_timeout = 900
# cpuexecutioncap 제거
```

### 배운 것
- **"자원을 아끼려는 설정"이 타임아웃의 원인**이 될 수 있습니다
- 오류 메시지만 보고 판단하지 말고 **실제 상태를 먼저 확인**해야 합니다

---

## 3. CRLF — `set: pipefail` 오류

### 증상
```
: invalid option nameine 9: set: pipefail
```

### 원인
PowerShell 이 Windows 방식(CRLF)으로 파일을 만들었고, Linux `sh` 가 `\r` 을 명령의 일부로 읽었습니다.

`set: pipefail` 이 아니라 `set: pipefail\r` 이 된 것입니다.

### 해결
```bash
sed -i 's/\r$//' /tmp/script.sh
```

**근본 대책 ①** — `.gitattributes` 에
```
*.sh text eol=lf
```

**근본 대책 ② — Vagrantfile 에서 자동 처리** *(팀원 제안, 2026-09-04 반영)*

Windows 에서 `git clone` 하면 git 설정(`core.autocrlf`)에 따라 `\r` 이 붙을 수 있습니다.
`.gitattributes` 만으로는 각자 환경을 타므로, **VM 안에서 한 번 더 정리**합니다.

```ruby
node.vm.provision "fix-line-endings", type: "shell",
  inline: "find /tmp/scripts -type f -name '*.sh' -exec sed -i 's/\\r$//' {} \\;"
```

🔴 **순서가 중요합니다** — `file` 다음, `common` 앞이어야 합니다.
뒤로 가면 이미 첫 스크립트가 깨진 뒤입니다.

> macOS · 리눅스 호스트에서는 애초에 `\r` 이 없어 아무 일도 하지 않습니다.

### 배운 것
- Windows ↔ Linux 파일 전송에서 흔한 함정입니다. 오류 메시지가 깨져 보이는 것도 특징입니다
- **호스트 환경에 의존하는 문제는 대상 쪽에서 정리**하는 편이 확실합니다.
  `.gitattributes` 는 각자 git 설정을 타지만, VM 안에서 `sed` 를 돌리면 언제나 같습니다

---

## 4. `registries.yaml` 배포 실패

### 증상
```
tee: /etc/rancher/k3s/registries.yaml: No such file or directory
```

그런데 루프의 `Write-Host "$n 완료"` 는 그대로 찍혀서 **성공한 것처럼 보였습니다.**

나중에 확인해보니:
```
node0 : /etc/rancher/k3s/registries.yaml
node1 : 없음
node2 : 없음   ...
```

### 원인
k3s **서버**는 설치 때 `/etc/rancher/k3s/` 를 만들지만, **에이전트는 만들지 않습니다.**

### 해결
```bash
sudo mkdir -p /etc/rancher/k3s
```

### 배운 것
- 스크립트가 "완료"를 찍어도 **실제 결과를 확인**해야 합니다
- 반복문에서 오류를 무시하면 **조용히 실패**합니다

---

## 5. `sudo k3s: command not found`

### 증상
```bash
$ sudo k3s kubectl get nodes
sudo: k3s: command not found
```
서비스는 `active (running)` 인데도 명령을 못 찾습니다.

### 원인
RHEL 계열의 `sudo` 는 보안상 `secure_path` 에서 `/usr/local/bin` 을 **제외**합니다.
k3s 는 거기 설치됩니다.

| | PATH |
|---|---|
| 일반 사용자 | `/usr/local/bin` 포함 ✅ |
| `sudo` | **제외** ❌ |

### 해결
```bash
sudo /usr/local/bin/k3s kubectl get nodes    # 전체 경로
```
또는 kubeconfig 를 설정해 `sudo` 없이:
```bash
mkdir -p ~/.kube
sudo cp /etc/rancher/k3s/k3s.yaml ~/.kube/config
sudo chown vagrant:vagrant ~/.kube/config
kubectl get nodes
```

### 배운 것
**EKS 워커 노드(Amazon Linux 2023)도 같습니다.** 알아두면 AWS 에서 헤매지 않습니다.

---

## 6. 🔴 노드 간 파드 통신 불가 — 최대 난관

### 증상
DB 를 만들려 하니 웹훅 호출이 타임아웃:
```
Error from server (InternalError): failed calling webhook "mcluster.cnpg.io":
context deadline exceeded
```

### 진단 — 하나씩 배제

| 확인 | 결과 |
|---|---|
| 노드 상태 | 6대 `Ready` ✅ |
| 오퍼레이터 파드 | `1/1 Running` ✅ |
| firewalld trusted 존 | `10.42/10.43` 등록됨 ✅ |
| firewalld 포트 | 6대 전부 `8472/udp` ✅ |
| **파드 직접 접속** | `curl https://10.42.4.2:9443/readyz` → **`exit=28`** 🔴 |

**방화벽도 정상, 라우팅도 정상, 파드도 정상인데 패킷만 안 갔습니다.**

```bash
$ ip -d link show flannel.1
vxlan id 1 local 10.0.2.15 dev enp0s3      # ← 여기서 원인 발견
```

### 원인

Vagrant VM 에는 NIC 이 두 개입니다.

| 인터페이스 | 주소 | 특징 |
|---|---|---|
| `enp0s3` | `10.0.2.15` | Vagrant NAT — **6대 전부 동일** |
| `enp0s8` | `192.168.56.x` | 사설망 — 노드별 고유 |

**Flannel 이 NAT 쪽을 잡았습니다.** 모든 노드가 같은 IP 로 VXLAN 터널을 맺으려 하니 통신이 되지 않습니다.

`--node-ip` 를 줬는데도 이렇게 된 이유 — 그건 **"쿠버네티스가 보는 노드 주소"만** 바꿉니다. CNI 가 쓰는 인터페이스는 별개입니다.

### 해결
```bash
--flannel-iface=enp0s8 --node-ip=192.168.56.1x
```

전 노드의 systemd 유닛을 수정하고 재시작했습니다.

**확인**
```
vxlan id 1 local 192.168.56.10 dev enp0s8      ← 바뀜

Received Subnet Event with VxLan: PublicIP: 192.168.56.10
Received Subnet Event with VxLan: PublicIP: 192.168.56.11
Received Subnet Event with VxLan: PublicIP: 192.168.56.12 ...

curl -k https://10.42.4.2:9443/readyz  →  OK  exit=0
```

### 배운 것

- **CNI 는 노드 IP 와 별개로 자기 인터페이스를 고릅니다.** NIC 이 여러 개인 환경의 흔한 함정입니다
- 증상이 **표면적 원인(방화벽)을 가리켜** 한 단계 더 파고들어야 했습니다
- 진단 순서: 노드 상태 → 방화벽 → **CNI 인터페이스**
- AWS EKS 는 VPC CNI 라 이 문제가 없지만, **"노드 간 통신이 안 될 때 어디를 봐야 하는지"** 는 그대로 쓰입니다

### ⚠️ 수정 중 겪은 실수

`sed` 로 systemd 유닛을 고칠 때 두 옵션이 **작은따옴표 하나 안에 묶였습니다.**
```
'--flannel-iface=enp0s8 --node-ip=192.168.56.10'    ← 통째로 한 인자
```
k3s 가 이걸 하나의 옵션 이름으로 읽어 기동에 실패합니다. 두 줄로 나눠야 했습니다.

---

## 7. CloudNativePG Secret 미생성

### 증상
```
Warning  Failed  kubelet  spec.containers{initdb}: Error: secret "reluxe-db-app" not found
→ CreateContainerConfigError
```

### 원인
`postgres-cluster.yaml` 의 주석에는 *"이 이름의 시크릿이 없으면 CNPG 가 자동 생성한다"* 고 되어 있었는데,
**`bootstrap.initdb.secret.name` 을 명시하면 "이미 있는 것을 쓰겠다"는 뜻**이 되어 자동 생성되지 않습니다.

### 해결
```bash
kubectl create secret generic reluxe-db-app -n reluxe \
  --type=kubernetes.io/basic-auth \
  --from-literal=username=reluxe \
  --from-literal=password="$(openssl rand -hex 16)"
```

> `--type=kubernetes.io/basic-auth` 가 중요합니다. CNPG 가 이 형식을 기대합니다.

### 배운 것
문서(주석)와 실제 동작이 다를 수 있습니다. **주석을 믿기 전에 한 번 확인**해야 합니다.

> ⚠️ 이 문제는 **2차 구축에서 다른 형태로 재발**합니다. (문제 12 참조)

---

## 8. Helm 훅 실행 순서

### 증상
```
Error: configmap "reluxe-config" not found
→ CreateContainerConfigError
```

### 원인

`migrate-job` 에 `pre-install` 훅을 걸었습니다. 마이그레이션이 앱보다 먼저 실행되어야 하기 때문입니다.

```
① migrate-job (pre-install 훅)   ← ConfigMap 을 찾는데 아직 없음
② ConfigMap · Secret · Deployment ...
```

**훅은 일반 리소스보다 먼저 실행**되므로, ConfigMap 이 아직 만들어지지 않은 상태였습니다.

### 임시 해결 (테스트 중)
ConfigMap 에도 훅을 걸어 `hook-weight: -10` 으로 먼저 만들게 함 → 동작 확인

### 🔴 최종 해결 (차트 수정)

임시 방편에는 두 가지 부작용이 있습니다.
- `deployment.yaml` 의 `checksum/config` 가 무의미해짐 → 값을 고쳐도 파드가 재시작 안 됨
- ConfigMap 이 릴리스에서 빠져 **`helm rollback` 대상이 아님**

그래서 **migrate-job 이 ConfigMap 을 선택적으로 참조**하도록 바꿨습니다.

```yaml
envFrom:
  - secretRef:
      name: reverdi-secret          # DATABASE_URL — 필수
  - configMapRef:
      name: reverdi-config
      optional: true                # 없으면 건너뜀
```

마이그레이션에 실제로 필요한 값은 `DATABASE_URL` 하나뿐이고 그건 Secret 에 있습니다.
`LOG_LEVEL` 등은 `config.py` 에 전부 기본값이 있어 없어도 동작합니다.

### 배운 것
- Helm 훅은 **일반 리소스보다 먼저** 실행됩니다
- **"돌아가게 하는 것"과 "제대로 고치는 것"은 다릅니다**

---

## 9. 🔴 파드가 한 노드에 몰림 — 설정은 맞는데 동작이 안 된 사례

### 증상
```
reverdi-web-...-6zbgh   node1
reverdi-web-...-9kljz   node1     ← 2개가 몰림
reverdi-web-...-rbf77   node2
                        node3 은 비어 있음
```

`topologySpreadConstraints` 를 넣었고, `whenUnsatisfiable` 을 `ScheduleAnyway` → `DoNotSchedule` 로 바꿔도 계속 몰렸습니다.

### 진단
```bash
kubectl get deploy reverdi-web -n reverdi \
  -o jsonpath='{.spec.template.spec.topologySpreadConstraints}'
```
```json
[{"labelSelector":{"matchLabels":{...}},"maxSkew":1,
  "topologyKey":"kubernetes.io/hostname","whenUnsatisfiable":"DoNotSchedule"}]
```
설정은 정확히 들어가 있었습니다.

### 원인

`labelSelector` 가 롤링 업데이트 중 **옛 파드까지 셉니다.**

```
스케줄 시점:  node3 에 옛 파드 있음 → "여긴 찼다" 판단 → node1 로
그 직후:      옛 파드 종료 → node3 가 비었지만 이미 늦음
```

**규칙 위반이 아니었습니다.** 옛 파드까지 세면 `maxSkew: 1` 이 지켜집니다.
**세는 대상이 잘못된 것**이었습니다.

### 해결
```yaml
matchLabelKeys:
  - pod-template-hash      # 같은 세대(ReplicaSet)끼리만 계산
```

`matchLabelKeys` 는 **이 문제 때문에 k8s 에 추가된 기능**이고, 1.27 부터 쓸 수 있습니다.

**결과**
```
reverdi-web-...-zdbl2   node1
reverdi-web-...-dgw5f   node2
reverdi-web-...-swsxv   node3      ← 하나씩
```

### 배운 것
- 설정이 들어갔다고 동작하는 게 아닙니다. **실제 결과를 확인**해야 합니다
- **AWS EKS 에서도 그대로 발생하는 문제**입니다

---

## 10. BOM — `control characters are not allowed`

### 증상
저장소 이름을 `reluxe` → `reverdi` 로 바꾼 뒤:
```
error: error parsing infra/postgres-cluster.yaml:
error converting YAML to JSON: yaml: control characters are not allowed
```

### 진단
```bash
$ file infra/postgres-cluster.yaml
infra/postgres-cluster.yaml: UTF-8 Unicode (with BOM) text
```

`grep -P '[\x00-\x1F]'` 로는 **안 잡혔습니다.** BOM 은 그 범위 밖입니다.

### 원인
PowerShell 로 파일을 고치면 **UTF-8 BOM(`EF BB BF`)이 자동으로 붙습니다.**
YAML 파서는 이걸 제어문자로 봅니다.

### 해결
```bash
# 탐지
head -c3 파일 | grep -q $'\xef\xbb\xbf' && echo BOM

# 제거
sed -i '1s/^\xEF\xBB\xBF//' 파일
```

**근본 대책** — PowerShell 에서 파일을 쓸 때
```powershell
[System.IO.File]::WriteAllText($path, $content, (New-Object System.Text.UTF8Encoding $false))
```
`$false` 가 BOM 없는 UTF-8 입니다.

### 배운 것
윈도우에서 파일을 고치면 **의도하지 않은 바이트가 붙습니다.**
`git diff` 로는 안 보이고, 파싱할 때야 드러납니다.
팀에 윈도우 사용자가 있으면 **VS Code 에서 "UTF-8(BOM 없음)"으로 저장**하도록 정해두는 게 좋습니다.

---

## 11. U+0080 — BOM 제거 후에도 같은 오류

### 증상
BOM 을 지웠는데도 같은 오류가 계속됐습니다.

| 검사 | 결과 |
|---|---|
| `grep -P '[\x00-\x1F]'` | 안 나옴 |
| `file` | "UTF-8 text" **정상** |
| `python3 -c "d.decode('utf-8')"` | **성공** |
| **파이썬 YAML 파서** | 🔴 `unacceptable character #x0080` |

### 원인
**`0x80~0x9F`(C1 제어문자)는 유효한 UTF-8 이지만 YAML 에서는 금지**입니다.
인코딩 변환 중 한글 주석 일부가 이 범위로 깨진 것이었습니다.

터미널에서 `?뵶 鍮꾨?踰덊샇` 처럼 보이던 게 신호였습니다.

### 해결
```bash
python3 -c "
import re, pathlib
pat = re.compile(r'[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x9F]')
for p in pathlib.Path('.').rglob('*.yaml'):
    s = p.read_text(encoding='utf-8')
    n = pat.sub('', s)
    if n != s: p.write_text(n, encoding='utf-8'); print('정리:', p)
"
```

### ⚠️ 이때 제가 만든 2차 사고

주석을 통째로 제거하는 스크립트를 만들었는데, **줄 끝 주석을 지우면서 줄바꿈까지 날아갔습니다.**

```yaml
configs: params:              ← 원래는 두 줄
server: replicas: 1 service:  ← 원래는 여러 줄
```

거기에 **git 병합 충돌 마커까지** 남아 있어서(문제 15) 파일이 완전히 망가졌습니다.
결국 **다시 작성하는 게 빨랐습니다.**

### 배운 것
- **`file` 명령과 UTF-8 디코딩 성공은 YAML 유효성을 보장하지 않습니다**
- 파이썬 YAML 파서로 확인하는 것이 가장 정확했습니다
- 자동 수정 스크립트는 **백업 후 결과를 눈으로 확인**해야 합니다

---

## 12. 🔴 CNPG 가 Secret 을 덮어씀

### 증상
```
Error: failed pre-install: job reverdi-migrate-1 failed: BackoffLimitExceeded
```

Helm 훅 Job 은 실패하면 파드가 정리되어 로그를 잡기 어려웠습니다.
**같은 이미지로 직접 돌려서** 오류를 봤습니다.

```bash
kubectl run dbtest -n reverdi --rm -it --restart=Never \
  --image=<레지스트리>/reverdi-backend:dev \
  --env="DATABASE_URL=postgresql+asyncpg://reverdi:<PW>@reverdi-db-rw.reverdi.svc:5432/reverdi" \
  -- alembic upgrade head
```
```
asyncpg.exceptions.InvalidPasswordError: password authentication failed for user "reverdi"
```

### 원인

| | |
|---|---|
| 우리가 만든 Secret | `1cd8ba9d...` |
| CNPG 가 만든 것 | `98f0ea7d...` |

`bootstrap.initdb.secret.name` 을 명시했는데도, **CNPG 가 초기화 시점에 Secret 을 새로 만들어 덮어썼습니다.**

> 문제 7 과 정반대 동작입니다. 1차에서는 "안 만들어준다"고 판단했는데,
> 실제로는 **초기화 시점에 새로 만든다**는 것이었습니다.
> 1차에서는 우연히 순서가 맞아떨어져 걸리지 않았습니다.

### 해결 — 순서를 바꿈

```
잘못  ① Secret 생성 → ② DB 생성 → ③ 앱 Secret
맞음  ① DB 생성 → ② healthy 대기 → ③ CNPG 비번 꺼내기 → ④ 앱 Secret
```

```bash
kubectl apply -f infra/postgres-cluster.yaml
kubectl get cluster -n reverdi                  # healthy 대기
PW=$(kubectl get secret reverdi-db-app -n reverdi -o jsonpath='{.data.password}' | base64 -d)
kubectl create secret generic reverdi-secret -n reverdi \
  --from-literal=DATABASE_URL="postgresql+asyncpg://reverdi:${PW}@..." ...
```

### 배운 것

- **오퍼레이터가 관리하는 리소스는 우리가 만든 것을 덮어쓸 수 있습니다**
- **재구축이 검증입니다.** 한 번 성공한 것은 우연일 수 있고, 두 번째에 진짜가 보입니다
- Helm 훅 실패는 **같은 이미지로 직접 돌려보는 것**이 가장 빠른 진단입니다

---

## 13. Jenkins — Pod Template 미생성

### 증상
```
Still waiting to schedule task
Waiting for next available executor
```
빌드가 시작되지 않고 무한 대기했습니다.

### 진단
Jenkins `Jenkins 관리` → `Clouds` → `kubernetes` → `Pod Templates`
```
No pod template added yet.
```

Script Console 로 확인:
```groovy
def c = jenkins.model.Jenkins.get().clouds.get(0)
println "templates: ${c.templates.size()}"
println "canProvision(build): ${c.canProvision(new hudson.model.labels.LabelAtom('build'))}"
```
```
containerCap: 10        정상
namespace: infra        정상
templates: 0            ← Pod Template 없음
canProvision(build): false
```

### 원인
`helm-values/jenkins.yaml` 에 이렇게 되어 있었습니다.
```yaml
agent:
  enabled: false   # Kubernetes 플러그인이 podTemplates로 동적 생성하므로 기본 agent는 끔
```

주석의 이해가 틀렸습니다. **`enabled: false` 는 에이전트 기능 전체를 끄는 것**이고,
`podTemplates` 자체가 렌더링되지 않습니다.

### 🔴 해결 — 파드 정의를 Jenkinsfile 안으로

`enabled: true` 로 고칠 수도 있었지만, **더 나은 방법**을 택했습니다.

```groovy
def BUILD_POD = '''
spec:
  nodeSelector: { workload: batch }
  tolerations:
    - { key: workload, operator: Equal, value: batch, effect: NoSchedule }
  containers:
    - name: python      # uv · ruff · pytest · alembic
    - name: postgres    # 테스트용 사이드카
    - name: buildah     # 이미지 빌드 (privileged)
    - name: tools       # helm · git
'''

pipeline {
    agent {
        kubernetes {
            yaml BUILD_POD
            defaultContainer 'python'
        }
    }
```

**장점**
- Helm values 의 `podTemplates` 에 의존하지 않음
- Jenkins 를 다시 깔아도 그대로 동작
- **설정이 코드에 남아 재현됨**

### 배운 것
Helm 차트의 `enabled` 플래그는 **하위 설정까지 통째로 끕니다.**

---

## 14. Argo CD toleration 누락

### 증상
```
Error: INSTALLATION FAILED: failed pre-install: 1 error occurred:
  * timed out waiting for the condition
```

### 원인
```bash
$ grep -n "nodeSelector\|toleration" helm-values/argocd.yaml
14:  nodeSelector:
```
**`nodeSelector` 만 있고 `tolerations` 가 없었습니다.**

node5 에 `workload=infra:NoSchedule` taint 가 걸려 있어서, 파드가 그 노드로 가려 해도
**거부당해 `Pending`** 에 머물렀습니다. 훅 Job 이 안 끝나니 타임아웃이 났습니다.

### 🔴 같은 문제를 세 번 겪었습니다

| 파일 | 시점 |
|---|---|
| `infra/registry.yaml` | 1차 검토 때 발견 |
| `infra/minio.yaml` | 1차 검토 때 발견 |
| **`helm-values/argocd.yaml`** | **CI/CD 도입 때 또 발견** |
| **`helm-values/kube-prometheus-stack.yaml`** | **모니터링 때 또 발견** (prometheus·grafana) |

### 해결
```yaml
global:
  nodeSelector: { workload: infra }
  tolerations:
    - { key: workload, operator: Equal, value: infra, effect: NoSchedule }
```

Argo CD 는 컴포넌트가 여러 개(controller·server·repoServer·redis)라 **`global.` 아래**에 넣어야 전부 적용됩니다.

### 배운 것

**`nodeSelector` 와 `tolerations` 는 짝입니다.**

| | 역할 |
|---|---|
| `nodeSelector` | **"이 파드를 어디로 보낼지"** — 파드 입장 |
| `taint`/`toleration` | **"이 노드에 누가 들어올지"** — 노드 입장 |

taint 가 걸린 노드를 `nodeSelector` 로 지목해도, toleration 이 없으면 **거부당합니다.**

> ⚠️ `node-exporter` 는 예외입니다. DaemonSet 이라 **전 노드**에 떠야 하는데
> node0·node4·node5 에 각각 다른 taint 가 있습니다. `operator: Exists` 로 전부 견디게 합니다.

---

## 15. git 병합 충돌 마커가 커밋됨

### 증상
```yaml
configs: params:
    server.insecure: true
<<<<<<< HEAD
=======
>>>>>>> e05a69b5189260097b5ed7d3697200aa11b5b5a4
```

### 원인
`git pull` 후 충돌을 해결하지 않고 그대로 커밋·push 한 것입니다.

### 해결
파일을 다시 작성했습니다. 충돌 부분만 고치는 것보다 빨랐습니다.

### 배운 것
- 커밋 전에 `git diff` 로 확인하는 습관이 필요합니다
- **YAML 파싱 검사를 CI 에 넣으면** 이런 게 자동으로 걸립니다

---

## 16. Jenkinsfile 이 옛 버전

### 증상
Pod Template 을 고쳤는데도 `canProvision: false` 가 계속됐습니다.

### 진단
```bash
$ grep -n "agent {" -A4 cx2/Jenkinsfile
18:    agent { label 'build' }     ← 옛 버전
$ grep -c "BUILD_POD\|reverdi-backend" cx2/Jenkinsfile
0
```

**수정본이 `CloudeDX` 저장소에 push 되지 않았습니다.**

`reluxe` 이름도 16곳 남아 있었고, 저장소 주소도 `reverdi-gitops.git` 으로 틀려 있었습니다.

### 원인
파일을 로컬에서 고쳤지만 **push 를 빠뜨렸습니다.**
Jenkins 는 `Pipeline script from SCM` 설정이라 **저장소의 파일을 매번 새로 읽습니다.**
로컬 수정은 반영되지 않습니다.

또한 저장소 이름을 `reluxe` → `reverdi` 로 바꾸면서
**Jenkinsfile 안의 이름들을 같이 안 고쳤습니다.**

### 해결
수정본을 push 한 뒤 다시 확인:
```bash
$ grep -c "BUILD_POD" cx3/Jenkinsfile
2
```

빌드를 다시 돌리니 파드가 생성됐습니다.
```
Created Pod: kubernetes infra/reverdi-ci-2-vjp69-k33qs-8krt3
Container [buildah] [jnlp] [postgres] [python] [tools]
```

### 배운 것
**Jenkins 는 저장소의 Jenkinsfile 을 읽습니다.** 로컬에서 고쳐도 push 하지 않으면 반영되지 않습니다.

---

---

## 17. 🔴 root PATH 에 `/usr/local/bin` 이 없음 — 재발

### 증상

전자동 스크립트로 처음부터 다시 구축하는 중, node0 에서 멈췄습니다.

```
node0: /tmp/scripts/10-k3s-server.sh: line 68: kubectl: command not found
The SSH command responded with a non-zero exit status.
```

### 원인

**문제 5 와 같은 원인입니다.**

Vagrant 의 shell provisioner 는 **root 로 실행**되는데, RHEL 계열의 root PATH 에는
`/usr/local/bin` 이 없습니다. k3s 는 거기 설치됩니다.

문제 5 에서 이미 겪고 문서에도 적었는데, **자동화 스크립트를 만들 때 반영하지 않았습니다.**

### 해결

```bash
/usr/local/bin/kubectl get nodes -o wide
```

또는 스크립트 상단에:

```bash
export PATH=$PATH:/usr/local/bin
```

### 배운 것

**알고 있어도 코드에 안 남기면 반복됩니다.**

문제 5 는 "손으로 명령을 칠 때" 겪었고, 그때는 `sudo /usr/local/bin/k3s` 로 넘겼습니다.
같은 함정이 **스크립트에서 다시 나온 것**은, 문서에만 적고 코드에는 안 옮겼기 때문입니다.

> 재발 방지는 문서가 아니라 **코드**에 넣어야 합니다.

한 가지 더 — 이 오류는 **화면 출력용 명령**에서 났습니다.
k3s 설치 자체는 성공했고 `systemctl is-active k3s` 는 `active` 였습니다.
그런데 `set -euo pipefail` 때문에 스크립트 전체가 실패로 처리되어 `vagrant up` 이 멈췄습니다.

**중요하지 않은 출력에서 전체가 멈추지 않게** 하려면 `|| true` 를 붙이는 방법도 있습니다.
다만 그러면 진짜 실패도 지나치므로, 이번에는 명령을 고치는 쪽을 택했습니다.

---

## 18. node-exporter 가 node0 에만 안 뜸

### 증상

모니터링을 올린 뒤 확인하니 **5개**만 있었습니다.

```
kps-prometheus-node-exporter-...  node1
kps-prometheus-node-exporter-...  node2
kps-prometheus-node-exporter-...  node3
kps-prometheus-node-exporter-...  node4
kps-prometheus-node-exporter-...  node5
                                  node0 없음
```

node-exporter 는 DaemonSet 이라 **전 노드에 하나씩** 떠야 합니다.
node0 이 빠지면 컨트롤 플레인 지표를 못 봅니다.

### 진단

```bash
kubectl get ds kps-prometheus-node-exporter -n monitoring \
  -o jsonpath='{.spec.template.spec.tolerations}' | python3 -m json.tool
```
```json
[{"effect": "NoSchedule", "operator": "Exists"}]
```

우리가 값 파일에 준 것은 이랬습니다.

```yaml
nodeExporter:
  tolerations:
    - { operator: Exists }
```

`effect` 를 안 썼는데, 렌더링 결과에는 `"effect": "NoSchedule"` 이 붙어 있습니다.

### 원인

node0 의 taint 는 이것입니다.

```
CriticalAddonsOnly=true:NoExecute
```

`NoSchedule` 만 견디는 toleration 으로는 **`NoExecute` 를 통과하지 못합니다.**

`operator: Exists` 에 **`effect` 를 명시하면 그 effect 만** 견딥니다.
`effect` 를 생략하면 모든 effect 를 견디는데, 차트가 기본값을 덧붙이면서 `NoSchedule` 로 좁혀졌습니다.

### 해결

**둘 다 명시**합니다.

```yaml
nodeExporter:
  tolerations:
    - { operator: Exists, effect: NoSchedule }
    - { operator: Exists, effect: NoExecute }
```

이미 뜬 클러스터에는 patch 로 적용했습니다.

```bash
kubectl patch ds kps-prometheus-node-exporter -n monitoring --type merge \
  -p '{"spec":{"template":{"spec":{"tolerations":[{"operator":"Exists","effect":"NoSchedule"},{"operator":"Exists","effect":"NoExecute"}]}}}}'
```

**결과** — 6개 전부.

```
node0  node1  node2  node3  node4  node5
```

### 배운 것

- **taint 의 `effect` 는 세 가지**입니다 — `NoSchedule` · `PreferNoSchedule` · `NoExecute`
- `operator: Exists` 만으로는 부족합니다. **어떤 effect 를 견딜지**가 별개입니다
- 차트가 기본값을 덧붙일 수 있으므로, **렌더링 결과를 확인**해야 합니다

> 이번에도 **"설정을 넣었는데 동작이 안 되는"** 유형입니다.
> 문제 9(`matchLabelKeys`)와 같은 성격입니다 — 값 파일만 보면 맞아 보입니다.

---

---

## 19. 🔴 Helm 서브차트에 값이 전달되지 않음

### 증상

문제 18 을 고쳤는데도 node-exporter 가 node0 에 안 떴습니다.

```bash
$ kubectl get ds kps-prometheus-node-exporter -n monitoring \
    -o jsonpath='{.spec.template.spec.tolerations}'
[{"effect": "NoSchedule", "operator": "Exists"}]
```

우리가 값 파일에 준 것은 이랬습니다.

```yaml
nodeExporter:
  tolerations:
    - { operator: Exists, effect: NoSchedule }
    - { operator: Exists, effect: NoExecute }
```

**렌더링 결과에는 `NoSchedule` 하나뿐**입니다. `NoExecute` 가 사라졌습니다.

### 원인

`kube-prometheus-stack` 은 node-exporter 를 **직접 만들지 않습니다.**
`prometheus-node-exporter` 라는 **별도 차트를 가져다 씁니다.**

서브차트에 값을 넘기려면 **서브차트 이름을 키로** 써야 합니다.

| 키 | 누가 읽나 |
|---|---|
| `nodeExporter.enabled` | 상위 차트 — 서브차트를 켜고 끔 |
| `nodeExporter.tolerations` | 🔴 **아무도 안 읽음 — 조용히 무시** |
| `prometheus-node-exporter.tolerations` | ✅ 서브차트로 전달됨 |

그래서 서브차트 **기본값**이 그대로 남았습니다.

```yaml
# prometheus-node-exporter/values.yaml 기본값
tolerations:
  - effect: NoSchedule
    operator: Exists
```

`NoSchedule` 만 견디니 `NoExecute` taint 가 걸린 node0 에는 못 뜹니다.

공식 저장소에 같은 이슈가 있습니다 — `.nodeExporter.tolerations` 오버라이드가
반영되지 않는다는 내용(#2081)과, NoExecute taint 노드에 안 뜬다는 내용(#1347)입니다.

### 해결

```yaml
# enabled 는 상위 차트가 읽는다 — 그대로 둔다
nodeExporter:
  enabled: true

# 🔴 실제 설정은 서브차트 이름으로 준다
prometheus-node-exporter:
  tolerations:
    - { operator: Exists, effect: NoSchedule }
    - { operator: Exists, effect: NoExecute }
```

### 어떻게 발견했나

**팀원이 찾았습니다.** 계속 오류가 나서 키 이름을 바꿔보니 동작했다고요.

우리는 `kubectl patch` 로 실행 중인 DaemonSet 을 직접 고쳐서 6개를 띄웠기 때문에
**값 파일이 애초에 안 먹고 있다는 걸 몰랐습니다.**
`vagrant destroy` 후 다시 돌리면 또 5개가 됐을 겁니다.

### 배운 것

- **Helm 차트에 서브차트가 있으면 키 이름을 확인**해야 합니다
  ```bash
  helm show chart <차트> | grep -A20 dependencies
  ```
- **값을 준다고 반영되는 게 아닙니다.** 렌더링 결과를 확인해야 합니다
  ```bash
  helm template <릴리스> <차트> -f values.yaml | grep -A5 tolerations
  ```
- `kubectl patch` 로 고치면 **당장은 되지만 원인이 가려집니다.**
  값 파일을 고치고 재배포해서 확인해야 합니다

### 이번에 함께 점검한 것

같은 유형의 실수가 다른 곳에도 있는지 전 파일을 확인했습니다.

| 차트 | 서브차트 | 우리가 쓴 키 | 판정 |
|---|---|---|---|
| kube-prometheus-stack | `prometheus-node-exporter` | `nodeExporter` | 🔴 **수정** |
| | `kube-state-metrics` | `kubeStateMetrics` | `enabled` 만 줘서 안전 |
| | `grafana` | `grafana` | 이름 같아 안전 |
| argo-cd | `redis-ha` | `redis-ha` | 이름 같아 안전 |
| jenkins | 없음 | — | 안전 |

**나머지는 문제없었습니다.**

---

---

## 20. 모든 워크로드가 관리자 비밀번호를 갖고 있었음

### 증상

증상이 없었습니다. **모든 것이 정상 동작했습니다.**

백엔드가 보안 점검(`docs/security.md`) 중에 발견해 알려줬습니다.

> `config.py` 가 임포트 시점에 관리자 비밀번호를 요구해서 **크롤러도 받아야 떴다.**
> 크롤러 하나가 뚫리면 관리자 비밀번호까지 같이 넘어가는 구조가 그래서 생겼다.

### 원인

우리 차트가 **모든 워크로드에 같은 Secret 을 통째로** 넘기고 있었습니다.

```yaml
{{- define "reverdi.envFrom" -}}
- secretRef:
    name: reverdi-secret     # ADMIN_PASSWORD · SESSION_SECRET 포함
{{- end }}
```

웹 · 크롤러 · 집계 · 백업 · 마이그레이션이 전부 이걸 썼습니다.

**앱이 그렇게 요구했기 때문**입니다. `config.py` 가 임포트 시점에 검사해서,
크롤러도 관리자 비밀번호가 없으면 뜨지 않았습니다.

### 백엔드가 앱을 먼저 고쳤습니다

거부 시점을 **그 값을 실제로 쓰는 모듈**로 옮겼습니다.

```python
# app/config.py — 기록만 하고 통과
def _secret_env(name, local_default, *, hint=""):
    ...
    _DEFAULTED_SECRETS[name] = hint
    return local_default

# app/auth.py — 웹만 임포트하는 자리에서 거부
require_secrets("ADMIN_PASSWORD", "CLIENT_PASSWORD", "SESSION_SECRET")
```

**웹은 여전히 CrashLoopBackOff 로 죽고, 크롤러는 `DATABASE_URL` 만으로 뜹니다.**

### 우리가 확인한 것

정말 필요 없는지 코드에서 봤습니다.

```bash
$ grep -rn "from app.auth" app/crawler/   # 결과 없음
$ grep -rn "from app.auth" alembic/       # 결과 없음
```

크롤러도 마이그레이션도 `app/auth.py` 를 임포트하지 않습니다.

### 해결

배치용 헬퍼를 따로 만들었습니다.

```
reverdi.envFrom        웹     — 전체 Secret
reverdi.envFromBatch   배치   — DB 접속만
```

| 워크로드 | 받는 Secret |
|---|---|
| `deployment` (웹) | `reverdi-secret` |
| `crawler-cronjob` · `aggregate-cronjob` · `pgdump-cronjob` | `reverdi-db-secret` |
| `migrate-job` | `reverdi-db-secret` |

**기본값은 꺼둡니다.**

```yaml
secretScope:
  perWorkload: false          # 켜기 전에 이미지 확인
  dbSecretName: reverdi-db-secret
```

🔴 **켜는 순서가 있습니다.** 백엔드가 명시했습니다.

> `secretScope.perWorkload: true` — **4번 패치가 머지된 이미지에서만.**
> 이전 이미지는 크롤러가 관리자 비밀번호 없이는 안 뜬다

### 배운 것

- **증상이 없는 문제**가 가장 찾기 어렵습니다. 전부 잘 돌고 있었습니다
- **앱 구조가 인프라 설정을 강제**하기도 합니다.
  앱이 "임포트 시점에 요구"하는 한, 우리가 Secret 을 나누고 싶어도 못 나눴습니다
- 그 반대도 성립합니다 — **앱이 먼저 고쳐지자 인프라가 나눌 수 있게** 됐습니다
- 켜고 끄는 스위치를 두면 **이미지 버전에 맞춰 단계적으로** 적용할 수 있습니다

---

---

# AWS 이관에서 겪은 문제

> 2026-09-06 · 로컬에서 검증한 것을 EKS 로 옮기며 만난 12건.
> **네 건 다 "로컬에서는 우리가 정할 수 있었는데 AWS 에서는 아닌 것"** 이었다.

---

## 21. eksctl 이 고른 AZ 와 노드그룹 AZ 가 어긋남

### 증상

`eksctl create cluster` 가 15분쯤 돌다 실패했다.

```
could not find private subnets for zones ["ap-northeast-2a"]
  allSubnets = {"ap-northeast-2b", "ap-northeast-2c", "ap-northeast-2d"}
Error: failed to create cluster "reverdi"
```

### 원인

`cluster.yaml` 의 노드그룹에 AZ 를 박아뒀다.

```yaml
availabilityZones: [ap-northeast-2a, ap-northeast-2b, ap-northeast-2c]
```

그런데 **eksctl 은 VPC 를 만들 때 AZ 를 스스로 고른다.**
서울에는 a·b·c·d 네 개가 있는데 이번엔 **b·c·d** 를 골랐다.

노드그룹이 `2a` 를 찾는데 그 AZ 에 서브넷이 없다.

**VPC 의 AZ 를 고정하지 않고 노드그룹의 AZ 만 고정한 것**이 문제였다.
둘 중 하나만 박으면 어긋난다.

### 해결

노드그룹의 `availabilityZones` 를 **지웠다.**

```yaml
# availabilityZones 없음 — ASG 에 맡긴다
```

- 웹은 관리형 노드그룹이라 **ASG 가 사설 서브넷 전체에 알아서 분산**한다
- 배치·인프라는 1대씩이라 어디 떠도 무방하다

**결과** — 웹 3대가 b·c·d 에 하나씩 떴다.

```
ip-...-165-159   web   ap-northeast-2b
ip-...-156-232   web   ap-northeast-2c
ip-...-104-84    web   ap-northeast-2d
```

### 배운 것

- **환경이 정하는 것을 코드에 박으면 깨진다.** 맡길 수 있으면 맡기는 편이 낫다
- 클러스터 스택은 `CREATE_COMPLETE` 로 남아서, **노드그룹만 다시 만들면** 됐다
  (`eksctl create nodegroup -f cluster.yaml`) — 15분을 5분으로 줄였다

---

## 22. RDS 에 PostgreSQL 17.2 가 없음

### 증상

CloudFormation 스택 생성이 실패했다.

```
Cannot find version 17.2 for postgres (Status Code: 400)
```

### 원인

로컬 CloudNativePG 의 **컨테이너 이미지 태그**를 그대로 가져다 썼다.

```yaml
imageName: ghcr.io/cloudnative-pg/postgresql:17.2   # 로컬
EngineVersion: "17.2"                                # AWS ← 이게 없다
```

**RDS 는 관리형이라 AWS 가 지원하는 마이너 버전만** 쓸 수 있다.
확인해보니 이랬다.

```bash
$ aws rds describe-db-engine-versions --engine postgres --region ap-northeast-2     --query "DBEngineVersions[?starts_with(EngineVersion,'17')].EngineVersion" --output text
17.5  17.6  17.7  17.8  17.9  17.10  17.11
```

`17.2` 는 이미 지원이 끝났다.

### 해결

**메이저 버전만** 지정한다.

```yaml
EngineVersion: "17"
```

AWS 가 그 시점의 최신 마이너(`17.9`)를 골랐다.

### 배운 것

- **컨테이너 이미지 태그와 관리형 서비스의 버전 목록은 다르다.**
  메이저는 같아도 마이너는 안 맞을 수 있다
- 마이너를 박으면 **시간이 지나 깨진다.** Jenkins 플러그인 `:latest` 와 같은 성격이다
- 실패한 스택은 `ROLLBACK_COMPLETE` 로 남아 같은 이름으로 다시 못 만든다.
  **지우고 다시 해야** 한다

---

## 23. 🔴 인증서 없이 HTTPS 리스너를 요청

### 증상

앱은 배포됐는데 **ALB 주소가 안 나왔다.**

```bash
$ kubectl get ingress -n reverdi
NAME          CLASS   HOSTS   ADDRESS   PORTS   AGE
reverdi-web   alb     *                 80      11m
                              ↑ 비어 있다
```

`describe` 를 보니 같은 오류가 계속 반복되고 있었다.

```
Failed deploy model due to ... CreateListener
  ValidationError: A certificate must be specified for HTTPS listeners
```

### 원인

도메인이 없어 `certificateArn` 을 비웠는데,
**어노테이션에는 HTTPS 443 이 그대로 남아 있었다.**

```yaml
certificateArn: ""                                    # 비움
alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
alb.ingress.kubernetes.io/ssl-redirect: "443"        # ← 짝이 안 맞는다
```

인증서 없이 HTTPS 리스너를 만들 수 없다.

템플릿(`ingress.yaml`)은 `certificateArn` 이 있을 때만 인증서 어노테이션을 붙이도록
잘 되어 있었는데, **`listen-ports` 와 `ssl-redirect` 는 값 파일에 하드코딩**되어 있었다.

### 해결

```yaml
alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
# ssl-redirect 제거
```

**결과** — 2분 뒤 ALB 주소가 나왔다.

### 배운 것

- **한쪽만 바꾸고 짝을 안 맞추면 깨진다.** 21번(AZ)과 같은 성격이다
- 이런 건 값 3개가 한 세트다: `certificateArn` · `listen-ports` · `ssl-redirect`
  **주석에 "도메인이 생기면 셋을 같이 바꾼다"** 고 적어뒀다
- `kubectl get ingress` 의 `ADDRESS` 가 비어 있으면 `describe` 의 `Events` 를 봐야 한다

---

## 24. 🔴 파드가 노드 IAM 역할로 S3 에 붙음 — 로컬에 없던 개념

### 증상

파드는 `Running` 인데 `0/1` 로 **Ready 가 안 됐다.** ALB 는 `503` 을 냈다.

로그에 원인이 그대로 있었다.

```
INFO  Found credentials from IAM Role: eksctl-reverdi-nodegroup-...-NodeInstanceRole
ERROR S3 저장소 확인 실패: s3://reverdi-uploads-... (AccessDenied)
```

DB 는 정상이었고 **S3 만** 막혔다.

### 원인

**`NodeInstanceRole`** 로 붙고 있었다. IRSA 가 아니라 **노드의 IAM 역할**이다.

`aws/cluster.yaml` 이 ServiceAccount 는 만들어뒀는데,

```bash
$ kubectl get sa -n reverdi
default         reverdi-batch      reverdi-web     ← 있다
$ kubectl get deploy reverdi-web -n reverdi -o jsonpath='{.spec.template.spec.serviceAccountName}'
                                                    ← 비어 있다
```

**차트에 `serviceAccountName` 을 넣을 자리가 아예 없었다.**
로컬에는 IRSA 라는 개념이 없으니 만들 이유가 없었던 것이다.

### 해결

차트에 자리를 만들었다.

```yaml
# values.yaml — 로컬은 비운다
serviceAccountName: ""
batchServiceAccountName: ""

# values-aws.yaml
serviceAccountName: reverdi-web        # S3 읽기/쓰기/삭제
batchServiceAccountName: reverdi-batch # S3 쓰기만
```

```yaml
# deployment.yaml
{{- if .Values.serviceAccountName }}
serviceAccountName: {{ .Values.serviceAccountName }}
{{- end }}
```

**결과**

```
INFO  S3 저장소 확인: s3://reverdi-uploads-... 에 쓰기·삭제 가능
```

### ⚠️ 고치는 중에 만난 함정 — `with` 블록

처음에 `serviceAccountName` 을 이 자리에 넣었다가 실패했다.

```yaml
{{- with .Values.nodeSelector }}
{{- if .Values.serviceAccountName }}      ← 여기
serviceAccountName: {{ .Values.serviceAccountName }}
{{- end }}
nodeSelector:
  {{- toYaml . | nindent 8 }}
{{- end }}
```

```
nil pointer evaluating interface {}.serviceAccountName
```

**`with` 안에서는 `.` 이 바뀐다.** `.Values` 가 그 문맥에 없다.

`spec:` 바로 아래, 어떤 `with` 도 시작되기 전으로 옮겨 해결했다.
(`$.Values` 로 최상위를 가리키는 방법도 있다)

### 배운 것

- **로컬에 없던 개념이 AWS 에서 필요해지면, 차트에 자리를 만들어야 한다.**
  값을 비워두면 로컬은 그대로 동작한다 — 렌더링 자체가 안 되니까
- IRSA 는 **파드마다 다른 권한**을 준다.
  크롤러가 뚫려도 S3 삭제는 못 하게 나눴다 (웹은 삭제 가능, 배치는 쓰기만)
- Helm 의 `with` 블록 안에서는 `.Values` 를 못 읽는다. **`$.Values`** 를 쓰거나 밖으로 뺀다

---

---

## 25. 🔴 EBS CSI 드라이버가 설치되지 않음 — 스크립트는 "성공"

### 증상

`80-monitoring.sh` 도 `90-jenkins.sh` 도 **정상 종료**했다.
그런데 파드가 안 떴다.

```
infra        jenkins-0                          0/2   Pending   12m
monitoring   kps-grafana-...                    0/3   Pending   12m
monitoring   prometheus-...-prometheus-0        0/2   Pending   12m
```

`describe` 를 보니 `Events: <none>` — 스케줄러가 시도조차 안 했다.

PVC 를 보고 알았다.

```
NAME      STATUS    STORAGECLASS
jenkins   Pending   gp3
```

```
Waiting for a volume to be created either by the external provisioner
'ebs.csi.aws.com' or manually by the system administrator.
```

### 원인

**EBS CSI 드라이버가 아예 없었다.**

```bash
$ aws eks list-addons --cluster-name reverdi --region ap-northeast-2
coredns  kube-proxy  vpc-cni        ← aws-ebs-csi-driver 가 없다
```

`cluster.yaml` 에 이렇게 썼다.

```yaml
iam:
  serviceAccounts:
    - metadata: { name: ebs-csi-controller-sa, namespace: kube-system }
      wellKnownPolicies:
        ebsCSIController: true      # SA 와 역할은 만들어졌다

addons:
  - name: aws-ebs-csi-driver
    serviceAccountRoleARN: null     # 🔴 이러면 애드온이 안 만들어진다
```

**애드온과 IRSA 는 별개다.** `iam.serviceAccounts` 에 선언해두면 자동으로
연결될 줄 알았는데, 애드온에는 역할 ARN 을 따로 줘야 한다.
`null` 을 주니 eksctl 이 애드온 생성을 건너뛰었다.

### 해결 과정 — 두 번 헤맸다

**① 역할 이름을 잘못 지정**

```bash
eksctl create iamserviceaccount ... --role-name AmazonEKS_EBS_CSI_DriverRole
# → "no tasks" (SA 가 이미 있어서 건너뜀)

eksctl create addon ... --service-account-role-arn arn:...:role/AmazonEKS_EBS_CSI_DriverRole
# → 그 역할이 실제로는 없다
```

컨트롤러가 **존재하지 않는 역할**을 맡으려다 `CrashLoopBackOff` 가 났다.

```bash
$ aws iam get-role --role-name AmazonEKS_EBS_CSI_DriverRole
NoSuchEntity: The role ... cannot be found
```

**② 실제 역할은 이미 있었다**

```bash
$ eksctl get iamserviceaccount --cluster reverdi --region ap-northeast-2
kube-system  ebs-csi-controller-sa  arn:aws:iam::...:role/eksctl-reverdi-addon-iamserviceaccount-kube-s-Role1-5dzo3QVlhbPi
```

`cluster.yaml` 이 만든 역할이다. **이름이 무작위**라 미리 적을 수 없었다.

### 최종 해결

```bash
# 실제 역할을 조회해서 애드온에 연결
EBS_ROLE=$(eksctl get iamserviceaccount --cluster reverdi --region ap-northeast-2 -o json \
  | python3 -c "import json,sys; print([x['status']['roleARN'] for x in json.load(sys.stdin) if x['metadata']['name']=='ebs-csi-controller-sa'][0])")

aws eks update-addon --cluster-name reverdi --addon-name aws-ebs-csi-driver \
  --service-account-role-arn "$EBS_ROLE" --region ap-northeast-2 --resolve-conflicts OVERWRITE

kubectl rollout restart deploy/ebs-csi-controller -n kube-system
```

**결과** — 컨트롤러 `6/6 Running`, PVC 3개 전부 `Bound`,
Jenkins · Prometheus · Grafana 가 한꺼번에 떴다.

`cluster.yaml` 에서 `serviceAccountRoleARN: null` 을 지우고,
**역할 조회·연결을 `20-addons.sh` 로 옮겼다.** 역할 이름이 무작위라 스크립트가 찾아야 한다.

### 배운 것

- 🔴 **"스크립트가 성공했다"와 "파드가 떴다"는 다르다.**
  Helm 은 릴리스가 설치되면 성공이다. 파드가 뜨는지는 안 본다
- **로컬 k3s 는 `local-path` 프로비저너가 내장**이라 이런 게 없었다.
  EKS 는 EBS CSI 를 직접 설치하고 IRSA 까지 붙여야 한다
- 증상이 없는 문제가 가장 찾기 어렵다. **`kubectl get pod -A | grep -v Running` 을 습관처럼** 봐야 한다
- eksctl 이 만드는 역할 이름은 무작위다. **미리 적을 수 없으면 조회해서 쓴다**

---

## 26. 크롤러가 노드보다 큰 메모리를 요구

### 증상

크롤러 파드가 26분째 `Pending` 이었다.

```
0/5 nodes are available: 1 Insufficient memory,
1 node(s) had untolerated taint {workload: infra},
3 node(s) didn't match Pod's node affinity/selector.
```

그런데 batch 노드는 **7% 밖에 안 쓰고 있었다.**

```
memory  104Mi (7%)
```

### 원인

```
t3.small 전체 메모리    1959220Ki  (1.87GB)
t3.small 할당 가능      1471796Ki  (1.40GB)   ← 시스템·kubelet 이 0.47GB
크롤러 requests.memory  2Gi
```

**요청이 노드 전체보다 크다.** 노드가 비어 있어도 절대 안 뜬다.

로컬 node4 는 **8GB** 였는데, AWS 로 옮기며 `t3.small`(2GB)로 줄였다.
크롤러가 Chromium 을 돌린다는 걸 감안하지 않았다.

### 해결

`t3.medium`(4GB · 할당 가능 3.3GB) 노드그룹을 새로 만들고 옛 것을 지웠다.

```bash
eksctl create nodegroup --cluster reverdi --region ap-northeast-2 \
  --name reverdi-batch2 --node-type t3.medium --nodes 1 \
  --node-volume-size 60 --node-labels workload=batch \
  --node-private-networking --managed

# ⚠️ CLI 로 만든 노드그룹에는 taint 가 안 붙는다
kubectl taint node -l eks.amazonaws.com/nodegroup=reverdi-batch2 \
  workload=batch:NoSchedule --overwrite

eksctl delete nodegroup --cluster reverdi --name reverdi-batch --region ap-northeast-2 --wait
```

### 배운 것

- **노드 크기를 정하기 전에 파드의 `requests` 를 확인**해야 한다
  ```bash
  helm template reverdi charts/reverdi -f values-aws.yaml | grep -A3 "requests:"
  ```
- 인스턴스 타입의 **표기 메모리와 할당 가능 메모리는 다르다.**
  `t3.small` 2GB 중 실제로 쓸 수 있는 건 1.4GB 다
- `Insufficient memory` 인데 노드가 비어 있으면, **요청이 노드보다 큰 것**이다
- eksctl CLI 로 만든 노드그룹에는 **taint 가 안 붙는다.** 따로 걸어야 한다

---

## 27. 🔴 주석 안의 Helm 문법이 렌더링을 깨뜨림

### 증상

Argo CD Application 을 등록했더니 `Unknown` 에서 멈췄다.

```
Failed to load target state: ... failed running helm:
Error: parse error at (reverdi/templates/deployment.yaml:66): missing value for with
```

`helm template` 을 직접 돌려도 같은 오류가 났다.

### 원인

66행이 이랬다.

```yaml
      #    ⚠️ 이 줄을 {{- with }} 블록 안에 넣으면 안 된다.
```

**24번을 고치면서 그 교훈을 주석으로 적었는데, 그 주석이 새 오류를 만들었다.**

🔴 **Helm 은 YAML 주석을 무시하지 않는다.**
템플릿 엔진이 먼저 파일을 읽고 `{{ }}` 를 전부 처리한 뒤, 그 결과를 YAML 로 넘긴다.
그래서 `#` 뒤에 있어도 `{{- with }}` 를 문법으로 읽는다.
값이 없으니 `missing value for with` 가 난다.

### 해결

주석에서 중괄호를 뺐다.

```yaml
      #    ⚠️ 이 줄을 with 블록 안에 넣으면 안 된다.
      #       with 안에서는 점(.)이 바뀌어 .Values 를 못 읽는다 (nil pointer).
      #
      #    ⚠️ 주석에도 중괄호 두 개를 쓰지 않는다.
      #       Helm 은 YAML 주석을 무시하지 않는다.
```

**검사 방법**

```bash
grep -rn "^\s*#.*{{" charts/reverdi/templates/
```

아무것도 안 나와야 한다.

### 배운 것

- **주석은 안전지대가 아니다.** 템플릿 엔진에게는 주석도 그냥 텍스트다
- Helm 주석은 `{{/* ... */}}` 를 쓴다. 이건 엔진이 실제로 무시한다
- 설명을 적을 때 **문법 예시를 그대로 쓰지 않는다.** 말로 풀어 쓴다
- 이 오류는 **로컬에서 안 걸렸다.** 주석을 나중에 추가했고,
  `helm template` 을 다시 안 돌려봤기 때문이다.
  **파일을 고치면 반드시 다시 검증**해야 한다

---

## 28. metrics-server 가 없어 HPA 가 동작하지 않음

### 증상

Argo CD 가 `Synced` 인데 `Degraded` 였다.
그런데 개별 리소스는 **전부 `Healthy`** 였다.

```
$ kubectl get hpa -n reverdi
NAME          TARGETS              MINPODS   MAXPODS   REPLICAS
reverdi-web   cpu: <unknown>/70%   3         6         3
```

### 원인

**EKS 는 metrics-server 가 기본 설치가 아니다.**

로컬 k3s 에는 내장이라 HPA 가 그냥 동작했다.
EKS 에서는 직접 설치해야 하는데, `20-addons.sh` 에 넣지 않았다.

HPA 가 CPU 를 못 읽어 `<unknown>` 이 되고, Argo CD 가 이를 `Degraded` 로 판정했다.

### 해결

```bash
kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
```

**결과** — `TARGETS` 가 숫자로 바뀌고 Argo CD 가 `Healthy` 가 됐다.

`20-addons.sh` 에 추가했다.

### 배운 것

- **서비스는 멀쩡한데 판정만 나쁜 경우**가 있다.
  웹 앱은 계속 정상 응답하고 있었다. HPA 가 못 늘릴 뿐이었다
- **k3s 내장 기능이 EKS 에는 없을 수 있다.**
  `local-path`(25번) · `metrics-server`(28번) 둘 다 같은 성격이다
- Argo CD 의 `Degraded` 는 **개별 리소스가 아니라 앱 전체 판정**일 수 있다.
  리소스가 전부 `Healthy` 인데 전체가 `Degraded` 면 HPA 같은 걸 의심한다

---

---

## 29. 빌드 파드가 노드 하나를 다 먹는다

### 증상

Jenkins 빌드 에이전트 파드가 뜨지 않았다. batch 노드를 2대로 늘려도 같았다.

```
0/6 nodes are available: 2 Insufficient cpu, 2 Insufficient memory
```

### 원인

빌드 파드 하나가 **컨테이너 5개**를 띄운다.

| 컨테이너 | CPU | 메모리 |
|---|---:|---:|
| python · postgres · buildah · tools · jnlp | **2.0 vCPU** | **4.3Gi** |

`t3.medium` 은 2 vCPU · 할당 가능 3.3GB. **파드 하나가 노드보다 크다.**
노드를 몇 대로 늘려도 안 뜬다.

로컬 node4 는 4 vCPU · 8GB 라 여유가 있었다.

### 해결

`requests` 를 낮췄다. `limits` 는 그대로 뒀다.

```yaml
buildah:  1000m/2Gi   -> 500m/1Gi
tools:     200m/512Mi -> 100m/256Mi
```

**합계 2.0/4.3Gi → 1.7/3.0Gi** — 들어간다.
피크에는 `limits`(buildah 4Gi)까지 쓴다.

### 배운 것

- **`requests` 합계가 노드보다 크면 노드를 늘려도 소용없다.** 파드는 쪼개지지 않는다
- 노드 크기를 정하기 전에 확인할 것:
  `helm template ... | grep -A3 "requests:"`

---

## 30. CPU 부족이 asyncpg 테스트를 깨뜨렸다

### 증상

테스트 5건이 실패했다. 전부 `test_ownership.py` 였다.

```
5 failed, 371 passed
RuntimeError: got Future ... attached to a different loop
```

### 처음 의심한 것

`conftest.py` 를 뜯어봤다. 픽스처가 이미 `engine.dispose()` 로 대응하고 있었다.
엔진이 두 개(`engine`·`read_engine`)인 것도 찾았지만 해당 없었다.

**GitHub Actions 에서는 같은 코드가 통과**하고 있었다.

### 진짜 원인 — CPU

29번을 고치며 `python` 컨테이너 CPU 를 **300m → 800m** 으로 올렸더니
**376건 전부 통과**했다.

| 환경 | CPU | 결과 |
|---|---|---|
| GitHub Actions | 2코어 | 통과 |
| Jenkins (300m) | 0.3코어 | **5건 실패** |
| Jenkins (800m) | 0.8코어 | **376건 통과** |

CPU 가 모자라면 커넥션 정리가 늦어져, 다음 테스트가
이전 이벤트 루프에 묶인 커넥션을 물고 간다.

### 배운 것

- 🔴 **자원 부족이 "코드 버그처럼 보이는" 오류를 만든다**
- 비동기 코드는 타이밍에 민감하다. CPU 를 조이면 경합이 드러난다
- **다른 환경(CI)에서 통과하는지** 먼저 보는 게 빨랐다

---

## 31. 🔴 sed 가 조용히 실패했다

### 증상

파이프라인이 **성공**했는데 배포가 안 됐다.

```
Finished: SUCCESS
태그 변경 없음. 커밋을 건너뛴다.
```

git 의 태그는 `latest` 그대로였다.

### 원인

```
+ sed -i s|^  tag: .*|  tag: "985a7bf"| values-aws.yaml
        ^ 따옴표가 벗겨졌다
```

**Groovy 의 삼중따옴표 문자열도 백슬래시를 이스케이프로 처리한다.**
Jenkinsfile 에 적은 이스케이프가 Groovy 에서 한 번 풀리고,
셸이 다시 처리하며 따옴표가 사라졌다.

따옴표 없는 `sed` 인자는 공백으로 쪼개져 `s|^` 만 스크립트가 됐다.

### 왜 못 잡았나 — 종료 코드 0

| 방어선 | 왜 못 잡았나 |
|---|---|
| `set -e` | sed 가 0 을 반환 |
| `git diff --quiet` | 정말 변경이 없으니 "없음"이 맞다 |
| 로그 | "태그 변경 없음" 은 정상처럼 보인다 |

**세 겹을 전부 통과한 실패**였다.

### 2차 시도도 실패

python 으로 바꿨더니 Groovy 파싱이 깨졌다.

```
WorkflowScript: 334: unexpected char: 0x5C
```

**같은 뿌리다.** 정규식의 백슬래시를 Groovy 가 이스케이프로 읽었다.

### 최종 해결 — 백슬래시를 안 쓴다

정규식 대신 문자열 함수를 쓰고, 따옴표·줄바꿈은 `chr()` 로 만든다.

```python
q = chr(34); nl = chr(10)
if line.lstrip().startswith("tag:"):
    out.append(pad + "tag: " + q + t + q); n += 1
if n == 0:
    sys.exit("tag 줄을 못 찾음: " + f)     # 🔴 명시적 실패
```

**바꾼 줄이 0 이면 종료 코드 1** 로 죽는다.

### 배운 것

- 🔴 **종료 코드 0 인 실패가 가장 위험하다**
- 파일을 바꾸는 명령은 **바꾼 개수를 세고, 0 이면 실패**시켜야 한다
- Groovy 문자열 안의 백슬래시는 **두 번 해석된다** (Groovy → 셸)
- 27번(주석 안의 Helm 문법)과 같다 — **"문자열 안이니 안전하겠지" 가 안 통한다**

---

## 32. 🔴 클러스터 삭제가 세 번 실패했다

### 증상

`99-destroy.sh` 를 돌렸는데 클러스터가 안 지워졌다.

```
Error: deadline surpassed waiting for AWS load balancers to be deleted:
       a528a6111b9ff4028a503b2e89a6c7e0
```

3일 뒤 확인하니 **노드 6대가 그대로 살아 있었다.** 그동안 요금이 계속 나갔다.

### 1차 원인 — 클래식 로드밸런서

```bash
$ aws elbv2 describe-load-balancers   # 비어 있음
$ aws elb   describe-load-balancers   # ← 여기 있었다
a528a6111b9ff4028a503b2e89a6c7e0
```

**클래식 LB(elb)와 ALB/NLB(elbv2)는 API 가 다르다.**
`elbv2` 만 보고 "없다"고 판단했다.

### 2차 원인 — 보안그룹 4개

LB 를 지우고 다시 시도했더니 VPC 에서 막혔다.

```
k8s-reverdi-reverdiw-67150beb75
k8s-elb-a528a6111b9ff4028a503b2e89a6c7e0
k8s-traffic-reverdi-9cc15bbbcb
k8s-monitori-kpsgrafa-6e7a00f1e1
```

**ALB 컨트롤러가 만든 보안그룹**이 남아 VPC 삭제를 막았다.

### 왜 남았나 — 순서가 문제였다

`99-destroy.sh` 는 `helm uninstall` 을 **맨 먼저** 했다.

```
① helm uninstall
     └ aws-load-balancer-controller 도 함께 사라진다
② kubectl delete ingress
     └ 지울 컨트롤러가 이미 없다 → LB·보안그룹이 남는다
```

Grafana 를 팀 공유용으로 `LoadBalancer` 로 바꿨던 게 발단이었다.
그때 만들어진 클래식 LB 와 보안그룹이 정리되지 않았다.

### 해결

```bash
# 1차 — 클래식 LB
aws elb delete-load-balancer --load-balancer-name a528a611... --region ap-northeast-2

# 2차 — 보안그룹
for sg in sg-0163a9d1... sg-0cc11865... sg-04334864... sg-08749ea2...; do
  aws ec2 delete-security-group --group-id $sg --region ap-northeast-2
done

# 3차 — 스택 재삭제
aws cloudformation delete-stack --stack-name eksctl-reverdi-cluster --region ap-northeast-2
```

### 스크립트 수정 — 순서를 바꿨다

```
전:  helm uninstall  →  Service·Ingress 삭제  →  클러스터
후:  Service·Ingress 삭제  →  대기  →  helm uninstall  →  클러스터
                                                          →  보안그룹 정리
                                                          →  스택 재삭제
```

**컨트롤러가 살아 있을 때 LB 를 정리**하도록 바꿨다.
LoadBalancer 타입 Service 를 ClusterIP 로 되돌리는 단계도 넣었다.

### 배운 것

- 🔴 **컨트롤러가 만든 리소스는 컨트롤러가 살아 있을 때 정리해야 한다**
- **클래식 LB 와 ALB 는 API 가 다르다.** `elb` 와 `elbv2` 를 둘 다 봐야 한다
- **"삭제 명령을 돌렸다" 와 "지워졌다" 는 다르다.**
  3일 뒤에야 알았다 — 삭제 후 반드시 확인해야 한다
- 삭제 스크립트도 **실패를 감지하고 재시도**해야 한다.
  `|| true` 로 넘기면 실패한 줄도 모른다

### Terraform 이면 어땠을까

```bash
terraform destroy
```

Terraform 은 **의존성 그래프로 역순 삭제**한다.
컨트롤러가 만든 리소스는 여전히 모르지만,
`kubernetes_service` 를 Terraform 이 관리하면 그것도 순서에 들어간다.

이번 일이 **Terraform 으로 옮기려는 이유** 중 하나가 됐다.

---

## AWS 이관 정리

### 네 건의 공통점

| # | 로컬에서는 | AWS 에서는 |
|:--:|---|---|
| 21 | AZ 를 우리가 정함 | **eksctl 이 고름** |
| 22 | 이미지 태그를 우리가 고름 | **AWS 가 지원하는 것만** |
| 23 | NodePort — 인증서 개념 없음 | **ALB 가 인증서를 요구** |
| 24 | 자격증명을 Secret 에 넣음 | **IRSA 로 파드마다 다름** |
| 25 | `local-path` 프로비저너 내장 | **EBS CSI 를 직접 설치 + IRSA** |
| 26 | node4 가 8GB | t3.small 은 **할당 가능 1.4GB** |
| 27 | (해당 없음) | 주석에 쓴 문법이 렌더링을 깨뜨림 |
| 28 | **metrics-server 내장** | **직접 설치** |
| 29 | node4 가 4 vCPU · 8GB | t3.medium 은 2 vCPU · 3.3GB |
| 30 | 자원 넉넉 | **CPU 부족이 테스트를 깨뜨림** |
| 31 | (해당 없음) | Groovy 이스케이프가 sed 를 깨뜨림 |
| 32 | `vagrant destroy` 한 줄 | **삭제 순서를 지켜야 한다** |

### 25 · 28 은 같은 성격이다

**k3s 가 내장하던 것이 EKS 에는 없다.**

| | k3s | EKS |
|---|---|---|
| 스토리지 프로비저너 | `local-path` 내장 | EBS CSI 직접 설치 |
| 메트릭 | metrics-server 내장 | 직접 설치 |

"쿠버네티스는 같다"고 생각했는데, **배포판이 무엇을 끼워주느냐가 달랐다.**

**"우리가 정할 수 있었던 것이 관리형 서비스에서는 아니다."**

해결도 같은 방식이었다 — **박지 말고 맡기거나, 자리를 만들어 값으로 넘긴다.**

### 차트는 한 줄도 안 바뀌었나

**아니다.** 24번에서 `serviceAccountName` 을 넣었다.

다만 **로컬에는 영향이 없다.** `values-vagrant.yaml` 에 값이 없어 렌더링되지 않는다.

```bash
$ helm template reverdi charts/reverdi -f values-vagrant.yaml | grep -c serviceAccountName
0
$ helm template reverdi charts/reverdi -f values-aws.yaml | grep -c serviceAccountName
3
```

**"환경 차이를 값으로 흡수한다"** 는 원칙이 여기서도 지켜졌다.

### 걸린 시간

| 단계 | 시간 |
|---|---|
| 클러스터 (실패 후 재시도 포함) | 35분 |
| 이미지 빌드·push | 30분 |
| RDS (실패 후 재시도 포함) | 40분 |
| 앱 배포 (ALB·IRSA 수정 포함) | 35분 |
| **합계** | **약 6시간** |

문제 8건을 만나지 않았다면 1시간 30분이었을 것이다.

---

## 정리

### ① 문서에 적어둔 함정이 실제로 나타났습니다

Rocky 박스 404, 공유 폴더, firewalld — **사전 조사한 항목이 그대로 발생**했습니다.
미리 조사하는 것이 시간을 아낍니다.

### ② 예상하지 못한 문제가 더 어려웠습니다

| 문제 | 공통점 |
|---|---|
| Flannel 인터페이스 (6) | 증상이 **방화벽**을 가리킴 |
| `matchLabelKeys` (9) | **설정은 맞는데** 동작 안 함 |
| CNPG Secret (12) | 1차에서는 **우연히 안 걸림** |

셋 다 문서에 없던 것이고 가장 오래 걸렸습니다.
**증상이 표면적 원인을 가리킨다**는 게 공통점입니다.

### ③ 같은 실수를 반복했습니다

**root PATH 에 `/usr/local/bin` 이 없는 것** — 두 번 (문제 5 → **17**)

특히 17번은 **문서에 적어두고도 코드에 안 옮겨서** 재발했습니다.
재발 방지는 문서가 아니라 **코드**에 넣어야 합니다.

**`nodeSelector` 만 넣고 `tolerations` 를 빠뜨린 것** — 네 번

`nodeSelector` 만 넣고 `tolerations` 를 빠뜨린 게 **registry · minio · argocd · prometheus/grafana**
네 곳에서 나왔습니다.

**패턴을 인지한 뒤로는 파일을 받자마자 먼저 확인**하게 됐습니다.

### ④ "돌아가게 하는 것"과 "제대로 고치는 것"은 다릅니다

ConfigMap 문제(8)를 훅으로 우회하면 당장은 돌아가지만 `helm rollback` 이 깨집니다.
부작용을 확인하고 다른 방법(`optional: true`)을 택했습니다.

### ⑤ 자동화가 진짜 검증입니다

손으로 한 번 성공한 뒤 스크립트로 만들었는데, **처음부터 다시 돌리자 두 곳에서 막혔습니다.**
둘 다 이미 알던 문제인데 코드에 안 남긴 것이었습니다.

> `vagrant destroy && vagrant up` 으로 **처음부터 재현되는지** 확인해야
> "된다"고 말할 수 있습니다.

### ⑥ 재구축이 검증입니다

이름을 바꾸며 처음부터 다시 세우자 **CNPG Secret 문제(12)가 드러났습니다.**
한 번 성공한 것은 우연일 수 있고, **두 번째에 진짜가 보입니다.**

### ⑦ 로컬 검증의 값어치

32개 문제를 에서 만났다면 클러스터 재생성에 20분씩 걸리고 비용도 나갔을 것입니다.
로컬에서는 `vagrant destroy && vagrant up` 으로 5분이면 처음부터 다시 할 수 있었습니다.

---

## 부록 — 진단 명령 모음

이번에 실제로 쓴 명령들입니다.

### 네트워크
```bash
# CNI 인터페이스 확인 (문제 6)
ip -d link show flannel.1 | grep vxlan
# → local 192.168.56.x 여야 정상

# 파드에 직접 접속
curl -k -m 5 https://<파드IP>:<포트>/readyz ; echo "exit=$?"
# → exit=28 이면 타임아웃 (통신 불가)

# 방화벽
sudo firewall-cmd --list-all --zone=trusted
sudo firewall-cmd --list-ports
```

### 파드가 안 뜰 때
```bash
kubectl describe pod -n <ns> <파드명> | tail -20
# Events 에 이유가 나온다

kubectl get events -n <ns> --sort-by=.lastTimestamp | tail -15
```

### Helm 훅 실패
```bash
# 같은 이미지로 직접 돌려본다 (문제 12)
kubectl run test -n <ns> --rm -it --restart=Never \
  --image=<이미지> --env="KEY=VALUE" -- <명령>
```

### YAML 검사
```bash
# BOM (문제 10)
file 파일.yaml                    # "(with BOM)" 확인
head -c3 파일.yaml | xxd

# 제어문자 (문제 11) — file 로는 안 잡힌다
python3 -c "
import yaml
yaml.safe_load(open('파일.yaml', encoding='utf-8'))
print('OK')
"

# git 충돌 마커 (문제 15)
grep -c '<<<<<<<\|>>>>>>>' 파일.yaml
```

### Jenkins
```groovy
// Script Console — Cloud 상태 (문제 13)
def c = jenkins.model.Jenkins.get().clouds.get(0)
println "templates: ${c.templates.size()}"
println "canProvision: ${c.canProvision(new hudson.model.labels.LabelAtom('build'))}"
```

### 자격증명 꺼내기
```bash
kubectl get secret <이름> -n <ns> -o jsonpath='{.data.<키>}' | base64 -d ; echo
```
