# AWS 구성도 — 설계 근거

> `AWS_구성도_최종.drawio` 4장에 담긴 판단을 설명합니다.
>
> **"왜 이렇게 했나"** 를 항목별로 적었습니다. 발표에서 질문이 나올 만한 곳 위주입니다.

---

## 0. 전체를 관통하는 판단 기준

설계할 때 세 가지를 기준으로 삼았습니다.

| 기준 | 뜻 |
|---|---|
| **우리가 서버를 운영하는가** | 관리형에 맡길 수 있으면 맡긴다 |
| **확장의 축이 같은가** | 늘어나는 이유가 다르면 분리한다 |
| **비용이 값어치를 하는가** | 이 규모에서 그 돈을 쓸 이유가 있는가 |

구성도에서 "AWS 관리형 서비스" 를 **한 상자에 모은 것**도 첫 번째 기준 때문입니다.
VPC 안팎을 나눈 게 아니라, **우리가 SSH 로 들어갈 수 있는가**로 나눴습니다.

---

# 1페이지 — 런타임 토폴로지

## 1-1. 왜 EKS 인가 (EC2 직접 운영이 아니라)

3차안까지는 **EC2 위에 Docker Compose** 였습니다. EKS 로 바꾼 이유는 셋입니다.

**① 파드 분산을 선언으로 표현할 수 있다**

```yaml
topologySpreadConstraints:
  - topologyKey: topology.kubernetes.io/zone
    maxSkew: 1
```

EC2 였다면 "AZ 마다 인스턴스를 하나씩 두고 ALB 에 등록" 을 손으로 해야 합니다.
**늘리고 줄일 때마다** 그 작업이 반복되고요.

**② 무중단 배포가 기본 동작이다**

```yaml
maxSurge: 1
maxUnavailable: 0
PodDisruptionBudget: minAvailable 2
```

Compose 로는 "새 컨테이너를 띄우고, 헬스체크하고, 옛것을 빼는" 절차를
스크립트로 써야 합니다.

**③ 배치와 웹을 같은 방식으로 다룬다**

크롤러는 CronJob, 웹은 Deployment 입니다. **같은 매니페스트 문법**으로 씁니다.

### 그럼 왜 ECS 가 아닌가

| | ECS | EKS |
|---|---|---|
| 학습 가치 | AWS 전용 | **쿠버네티스 표준** |
| 비용 | 컨트롤 플레인 무료 | 월 $73 |
| 생태계 | 제한적 | Helm · Argo CD · Prometheus |

**월 $73 을 학습 가치와 맞바꿨습니다.** 국비 과정 프로젝트라
"쿠버네티스를 다뤄봤다" 가 ECS 보다 값어치가 있다고 판단했습니다.

---

## 1-2. 왜 노드 그룹을 나눴나

구성도에 네 가지 이유를 적어뒀습니다.

| # | 이유 | 구체적으로 |
|:--:|---|---|
| ① | **자원 격리** | Chromium 이 메모리를 다 먹어도 웹 응답이 안 흔들린다 |
| ② | **이미지 크기** | 웹 노드가 2.7GB 크롤러 이미지를 받을 이유가 없다 |
| ③ | **확장 축이 다름** | 웹은 요청량(HPA), 배치는 스케줄 |
| ④ | **네트워크 방향** | 웹은 인바운드, 크롤러는 아웃바운드 |

### ③ 이 가장 중요합니다

**같은 노드에 두면 확장이 서로를 방해합니다.**

```
요청이 늘어 HPA 가 웹 파드를 늘림
   ↓
그 노드에 크롤러도 떠 있음
   ↓
자원이 모자라 웹 파드가 Pending
```

노드를 나누면 **각자의 이유로 각자 늘어납니다.**

### taint 로 강제하는 이유

```yaml
label:  workload=batch
taint:  workload=batch:NoSchedule
```

**라벨만으로는 부족합니다.** 라벨은 "여기로 가라"는 힌트고,
taint 는 "허락받은 것만 들어와라"는 강제입니다.

웹 파드에 `nodeSelector: workload=web` 만 걸면,
**배치 노드에 자리가 없을 때** 스케줄러가 웹 노드로 밀어 넣을 수 있습니다.
반대 방향(웹 파드가 배치 노드로)은 taint 가 막습니다.

---

## 1-3. 왜 NAT Gateway 1개인가

구성도에 근거를 숫자로 적어뒀습니다.

```
인터페이스 VPC 엔드포인트 — AZ 당 시간당 $0.01
6종 × 3AZ = 월 $131
```

**NAT 1개는 월 $32** 입니다. 4배 차이입니다.

### 무엇을 포기했나

**AZ-a 가 죽으면 파드의 아웃바운드가 끊깁니다.**

| 영향 받는 것 | 영향 없는 것 |
|---|---|
| 크롤링 | **사용자 요청 (ALB → 파드)** |
| SQS 발신 | DB 접근 |
| 이미지 pull | 파드 간 통신 |

🔴 **사용자 요청 경로에는 NAT 이 없습니다.** 들어오는 트래픽은 ALB → 파드 직행입니다.

그래서 구성도에 이렇게 적었습니다.

> AZ 1곳 = 남겨둔 SPOF

**의도적으로 남긴 단일 장애점**이라는 뜻입니다. 모르고 놔둔 게 아니고요.

### S3 만 게이트웨이 엔드포인트를 쓰는 이유

```
게이트웨이형 (S3 · DynamoDB)  →  요금 없음
인터페이스형 (그 외 전부)      →  AZ 당 시간당 $0.01
```

**공짜인 것만 썼습니다.** ECR 이미지 레이어와 `pg_dump` 업로드가
NAT 을 안 타므로 데이터 전송료도 아낍니다.

---

## 1-4. 왜 nginx 를 뺐나

3차안에는 nginx 가 있었습니다. 없앤 이유입니다.

| nginx 가 하던 일 | 대체 |
|---|---|
| 정적 파일 서빙 | **FastAPI StaticFiles** |
| 보안 헤더 | ALB + 앱 미들웨어 |
| TLS 종료 | **ACM + ALB** |
| 리버스 프록시 | ALB 가 파드로 직접 |

**ALB 가 TLS 를 끊어주면 nginx 가 할 일이 거의 없습니다.**

### 얻은 것

```
이미지 3종 → 2종        빌드·저장·배포가 줄었다
CORS 소멸               화면과 API 가 같은 출처
컨테이너 1개            로그·디버깅이 단순
```

FastAPI 가 `/` 에 `web/` 을 마운트하고, `/api/*` 는 JSON 을 줍니다.
**브라우저 입장에서 한 주소**라 CORS 설정 자체가 필요 없습니다.

---

## 1-5. 왜 대상그룹 타입이 `ip` 인가

```yaml
alb.ingress.kubernetes.io/target-type: ip
```

**VPC CNI 가 파드에 VPC IP 를 직접 줍니다.** 그래서 ALB 가 파드로 바로 분산합니다.

### `instance` 였다면

```
ALB → 노드 NodePort → kube-proxy → 파드
                       ↑ 홉이 하나 더
```

노드를 거치면 **다른 노드의 파드로 다시 넘어갈 수** 있습니다.
지연이 늘고, 어느 파드가 받았는지 추적이 어려워집니다.

`ip` 는 **ALB 가 파드를 직접 알기 때문에** 그 홉이 없습니다.

---

## 1-6. 왜 헬스체크가 `/health` 인가 (`/ready` 가 아니라)

구성도에 이렇게 적어뒀습니다.

> 헬스체크 `/health` — DB 미확인

### `/ready` 를 쓰면 생기는 일

```
DB 장애
   ↓
파드 3개의 /ready 가 동시에 실패
   ↓
ALB 가 대상그룹에서 전부 제외
   ↓
502 — 조회조차 안 된다
```

**DB 가 죽어도 읽기 복제본으로 조회는 됩니다.**
그런데 헬스체크가 DB 를 보면, 그 가능성을 스스로 없앱니다.

| 경로 | 확인하는 것 | 쓰는 곳 |
|---|---|---|
| `/health` | 프로세스가 살아있는가 | **ALB 헬스체크** |
| `/ready` | DB · S3 · 마이그레이션 | 쿠버네티스 readinessProbe · 사람이 확인 |

---

## 1-7. 왜 RDS 를 쿠버네티스 밖에 두나

로컬에서는 CloudNativePG 로 **클러스터 안**에 뒀습니다. AWS 에서는 밖입니다.

### 이유

**① 백업이 관리형으로 제공된다**

CloudNativePG 는 백업을 우리가 설정해야 합니다.
RDS 는 자동 백업 · PITR · 스냅샷이 기본입니다.

**② 클러스터를 지워도 DB 는 남는다**

`eksctl delete cluster` 로 클러스터를 날려도 RDS 는 별개입니다.
실험이 쉬워집니다.

**③ 페일오버가 검증된 동작이다**

CloudNativePG 도 자동 승격을 하지만, **엔드포인트 이름이 유지되는 건 RDS 의 장점**입니다.
앱은 재연결만 하면 됩니다.

### 그럼 로컬에서 CloudNativePG 를 쓴 건 낭비인가

아닙니다. **읽기/쓰기 분리와 페일오버 동작을 미리 확인**했습니다.

```
로컬  reverdi-db-rw / reverdi-db-ro
AWS   WriterEndpoint / ReaderEndpoint
```

앱 코드는 **그대로**입니다. 매니페스트만 버려집니다.

---

## 1-8. 왜 스탠바이와 읽기 복제본을 둘 다 두나

혼동하기 쉬운 부분입니다.

| | 동기 스탠바이 | 읽기 복제본 |
|---|---|---|
| 복제 방식 | **동기** (RPO 0) | 비동기 |
| 읽기 | ❌ **불가** | ✅ 가능 |
| 자동 승격 | ✅ | ❌ 수동 |
| 목적 | **고가용성** | **조회 오프로딩** |

### 스탠바이만 있으면

주 DB 가 전환되는 60~120초 동안 **조회도 멈춥니다.**
스탠바이는 읽기를 안 받으니까요.

### 읽기 복제본만 있으면

주 DB 가 죽으면 **쓰기가 영영 안 됩니다.** 수동 승격이 필요합니다.

### 둘 다 두면

```
주 DB 전환 중
  쓰기 → 잠시 끊김 (60~120초)
  조회 → 복제본이 받는다 (무중단)
```

**페일오버 시연에서 200 이 유지되는 이유**가 이겁니다.

---

## 1-9. 왜 ElastiCache 를 넣었나

구성도에 두 가지 용도를 적었습니다.

**① 클릭 중복 1차 필터**

```
SETNX click:{item}:{session}  TTL 30분
```

같은 사람이 같은 상품을 여러 번 눌러도 **DB 까지 안 갑니다.**

**② 분산 락**

README 에 이런 TODO 가 있었습니다.

> `_should_crawl_now()` 는 진짜 잠금이 아니다

파드가 3개면 **크롤링이 동시에 3번** 돌 수 있습니다. Redis 락으로 막습니다.

### 월 $10 의 값어치

`cache.t4g.micro` 단일 노드입니다. **고가용성을 포기**했습니다.

캐시가 죽으면:
- 클릭 중복 필터가 안 돌아 DB 유니크 제약이 막습니다 (느려질 뿐)
- 크롤링 락이 없어 중복 실행 가능

**서비스가 멈추지는 않습니다.** 그래서 단일 노드로 뒀습니다.

---

## 1-10. 왜 운영 EC2 를 클러스터 밖에 두나

구성도에 명확한 근거가 있습니다.

> k6 부하 생성기 — **클러스터 안에서 걸면 자기 자원을 먹어 측정이 오염된다**

부하를 만드는 쪽과 받는 쪽이 같은 자원을 쓰면 측정이 무의미해집니다.

Grafana 도 같은 이유입니다.

> **관측의 독립성** — 클러스터가 아플 때 대시보드도 같이 아프면 안 된다

---

# 2페이지 — 백업 · 복구

## 2-1. 왜 백업이 3중인가

**각각 다른 실패를 막습니다.**

| 백업 | 막는 사고 | 못 막는 것 |
|---|---|---|
| 자동 백업 + PITR | 실수로 DELETE · 잘못된 마이그레이션 | 인스턴스 삭제 |
| 수동 스냅샷 | 마이그레이션 실패 | 계정 사고 |
| **pg_dump → 우리 S3** | **계정·리전 사고** | — |

### pg_dump 가 왜 따로 필요한가

구성도에 이유를 적어뒀습니다.

> RDS 자동 백업·스냅샷은 **AWS 가 관리하는 저장소**에 있습니다.
> 우리 S3 버킷이 아닙니다.

계정에 문제가 생기면 **RDS 백업도 함께 영향**을 받습니다.
우리 버킷의 덤프가 **마지막 방어선**입니다.

로컬·다른 리전에서도 복원할 수 있고요.

---

## 2-2. 왜 복제본이 백업을 대신 못 하나

구성도의 표에 이 줄이 있습니다.

> **데이터 손상이 복제까지 전파** → 복제는 손상까지 복제합니다

```
잘못된 UPDATE 실행
   ↓
스탠바이도 같은 UPDATE 를 받는다
읽기 복제본도 받는다
   ↓
어디를 봐도 손상된 데이터
```

**시점 복원(PITR)만이 유일한 수단**입니다.

"복제본이 있으니 백업은 필요 없다" 는 흔한 오해입니다.

---

## 2-3. 왜 복구 훈련을 강조하나

> **"백업이 있다" 와 "복구해봤다" 는 다르다**

훈련하지 않으면 이런 것들을 모릅니다.

| 모르는 것 | 왜 문제인가 |
|---|---|
| 복원에 몇 분 걸리는지 | RTO 를 말할 수 없다 |
| 복원 후 앱이 붙는지 | 엔드포인트가 바뀐다 |
| 어떤 권한이 필요한지 | 사고 중에 막힌다 |

### 훈련 절차를 숫자로

```
① 현재 시각 기록 → 테스트 데이터 삽입
② 일부러 DELETE (사고 재현)
③ PITR 로 ② 직전 시점 복원
④ 복원된 인스턴스 검증
⑤ 걸린 시간 = RTO
```

**측정한 숫자가 곧 RTO** 입니다. 추측이 아니고요.

---

# 3페이지 — CI/CD

## 3-1. 왜 OIDC 인가 (액세스 키가 아니라)

워크플로 주석에 판단이 적혀 있습니다.

> 키가 유출되면 **회수 전까지 계속 위험**하다

| | 장기 액세스 키 | OIDC |
|---|---|---|
| 유효 기간 | 무기한 | **요청마다 임시 토큰** |
| 유출 시 | 회수할 때까지 위험 | 이미 만료 |
| 저장 위치 | GitHub Secrets | **저장 안 함** |

**저장할 게 없는 것**이 가장 큰 이점입니다.

---

## 3-2. 왜 테스트에 실제 Postgres 를 쓰나

구성도에 근거가 있습니다.

> SQLite 로 대체 안 함 — `ON CONFLICT` · `timestamptz` · `ilike` 가 검증 대상

**SQLite 에서 통과해도 Postgres 에서 깨지는 것들**입니다.

| 기능 | SQLite | Postgres |
|---|---|---|
| `ON CONFLICT ... DO UPDATE` | 문법이 다름 | 클릭 집계의 핵심 |
| `timestamptz` | 타임존 개념 없음 | 시각 비교 |
| `ilike` | 미지원 | 대소문자 무시 검색 |

**테스트가 무엇을 검증하는지**를 기준으로 정했습니다.

---

## 3-3. 왜 마이그레이션을 앱 교체보다 먼저 하나

구성도에 이유가 적혀 있습니다.

> 웹 파드 3개가 동시에 alembic 을 돌리면 **경합**한다

### 만약 앱 시작 시 마이그레이션을 돌리면

```
파드 A 시작 → alembic upgrade
파드 B 시작 → alembic upgrade   ← 동시 실행
파드 C 시작 → alembic upgrade
```

alembic 이 락을 쓰지만, **타이밍에 따라 실패**할 수 있습니다.

### 그래서 Job 으로 분리

```
① 수동 스냅샷
② Job (alembic upgrade head) — 하나만 실행
③ 완료 대기
④ 이미지 교체 (롤링)
```

**순서가 보장됩니다.**

로컬 차트에서는 이걸 **Helm 훅**으로 구현했습니다 (`pre-install`, `pre-upgrade`).

---

## 3-4. 왜 롤백 한 줄이 필요한가

```bash
kubectl rollout status --timeout=180s || kubectl rollout undo
```

구성도에 이렇게 적었습니다.

> k8s 에는 ECS 서킷 브레이커 같은 **자동 롤백이 없다.** 이 한 줄이 그 역할을 한다

ECS 는 배포가 실패하면 알아서 되돌립니다.
쿠버네티스는 **실패한 채로 그냥 둡니다.** 새 파드가 안 뜨고 옛 파드가 남을 뿐이죠.

`|| rollout undo` 가 그 빈자리를 메웁니다.

---

# 4페이지 — 모니터링

## 4-1. 왜 Prometheus 는 안, Grafana 는 밖인가

구성도에 원칙이 있습니다.

> **수집은 대상 가까이** · **관측은 독립적으로**

| | 위치 | 이유 |
|---|---|---|
| Prometheus | 클러스터 안 | 파드·노드 지표를 긁으려면 안에 있어야 |
| **Grafana** | **클러스터 밖** | **클러스터가 아플 때 대시보드도 아프면 안 된다** |

### 실제로 이런 일이 납니다

```
노드 자원 고갈
   ↓
Grafana 파드가 축출(evict)
   ↓
무슨 일이 벌어지는지 볼 수가 없다
```

**가장 필요할 때 없는 도구**가 됩니다.

---

## 4-2. 왜 경보를 "증상 기준" 으로 하나

구성도에 이렇게 적었습니다.

> **증상 기준 경보 (원인 기준 아님)**

| 원인 기준 (나쁨) | 증상 기준 (좋음) |
|---|---|
| CPU > 80% | **요청 실패율 > 1%** |
| 메모리 > 90% | **p95 > 500ms** |
| 파드 재시작 | HPA 가 max 에 붙어 있음 |

### 왜 그런가

**CPU 가 80% 여도 사용자가 멀쩡하면 문제가 아닙니다.**
반대로 CPU 가 30% 인데 응답이 느릴 수도 있고요.

원인 기준으로 걸면 **울리지 않아도 될 때 울립니다.**
그러면 사람이 경보를 무시하게 되고, 진짜 문제도 놓칩니다.

---

## 4-3. 왜 SLO 를 숫자로 정하나

> 대시보드가 있는 것과 **목표를 숫자로 정한 것**은 다르다

| | 예 |
|---|---|
| SLI (측정) | 가용성 = 2xx/전체 · 지연 = p95 |
| **SLO (목표)** | **조회 가용성 99.5% · p95 500ms** |
| 에러 버짓 | 99.5% 면 한 달에 3.6시간 |

**숫자가 있어야 판단할 수 있습니다.**

> "느린 것 같다" → 판단 불가
> "p95 가 700ms 로 목표를 넘었다" → 조치

---

## 4-4. 왜 CloudWatch 도 쓰나

Prometheus 로 다 되지 않나 싶지만, **볼 수 없는 영역**이 있습니다.

| CloudWatch 만 아는 것 |
|---|
| ALB 요청 수 · 5xx · 대상 응답 시간 |
| RDS CPU · 커넥션 · **ReplicaLag** |
| **페일오버 이벤트** |
| 백업 성공/실패 |
| ElastiCache 접속 수 |

**관리형 서비스는 우리가 에이전트를 못 심습니다.** AWS 가 주는 지표를 봐야 합니다.

그래서 Grafana 에 **데이터소스를 2개** 붙였습니다.

---

## 4-5. 왜 태그 규칙을 먼저 정하나

> 태그가 없으면 **무엇을 지울지 모르는 상태**가 된다.
> 첫 리소스를 만들기 전에 규칙을 정할 것.

리소스가 30개쯤 되면 **뭐가 뭔지 알 수 없습니다.**

```
Project     · reverdi
Environment · demo
Owner       · <담당자>
```

비용도 태그로 갈라 봅니다. "누가 뭘 얼마나 쓰는지" 가 보이고요.

---

# 마무리 — 이 설계가 남긴 것

## 의도적으로 남긴 약점

발표에서 "완벽하냐" 는 질문이 나오면 **이걸 먼저** 말하는 게 낫습니다.

| 남긴 것 | 왜 |
|---|---|
| **NAT 1개 (AZ-a SPOF)** | 월 $99 절약 · 사용자 경로엔 영향 없음 |
| ElastiCache 단일 노드 | 죽어도 서비스는 유지 |
| 리전 장애 대응 | 비용·시간 고려 — 설계 항목으로만 |
| 앱 레벨 호출 제한 | 파드 수만큼 느슨 — WAF 몫 |

**"알고 있으며 우선순위의 문제"** 라고 말할 수 있는 게 강합니다.

## 3차안(EC2 + Compose)에서 무엇이 나아졌나

| | 3차안 | 최종 |
|---|---|---|
| 배포 | 운영 서버에서 스크립트 | **CI 가 직접** |
| DB 백업 | 우리가 만들어야 | **관리형 + 훈련** |
| 로그 | 컨테이너 로테이션 | **CloudWatch 중앙** |
| 배포 서버 | 필요 | **불필요** |
| nginx | 있음 | **제거** (3종 → 2종) |

## 로컬 검증과의 관계

구성도는 **AWS 목표 구성**이고, 로컬(Vagrant + k3s)에서 먼저 검증했습니다.

| 로컬에서 확인한 것 | AWS 에서 그대로 |
|---|---|
| 파드 AZ 분산 | ✅ topologyKey 만 교체 |
| 무중단 배포 | ✅ |
| 읽기/쓰기 분리 | ✅ |
| 마이그레이션 훅 | ✅ |
| 페일오버 시 조회 유지 | ✅ |

**Helm 차트 `templates/` 는 한 줄도 안 바꿨습니다.**

로컬에 없던 개념 — IRSA · EBS CSI · ALB 컨트롤러 · ECR 인증 —
은 AWS 에서 처음 만났고, 그건 로컬로 대비할 수 없는 부분이었습니다.
