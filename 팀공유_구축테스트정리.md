# 구축 테스트 정리 (팀 공유)

> Vagrant + Rocky Linux 9 + k3s 로컬 클러스터에 Reverdi 를 올리고
> **앱 · DB 고가용성 · CI/CD · GitOps · 모니터링까지 전부 동작 확인**한 기록입니다.
>
> 📊 도표 — `구축테스트_정리.drawio` (3장)
> 📋 문제 상세 — `문제해결_기록.md` (24건)

---

## 0. 세 줄 요약

- **VM 6대에 클러스터를 세우고** 앱 · DB HA · Jenkins · Argo CD · Prometheus/Grafana 를 전부 올렸습니다.
- **구축 중 24건의 문제**를 만났고, 그중 13건은 우리 차트·스크립트의 실수라 **코드를 고쳤습니다.**
- **`vagrant up` 한 번으로 재현되는 자동화 스크립트**를 만들어뒀습니다.

---

## 1. 최종 구성

> 📊 도표 1페이지 참조

```
node0  192.168.56.10   4GB    컨트롤 플레인 전용 (taint 격리)
node1  192.168.56.11   4GB    웹 파드 + DB 파드
node2  192.168.56.12   4GB    웹 파드 + DB 파드
node3  192.168.56.13   4GB    웹 파드 + DB 파드
node4  192.168.56.14   8GB    크롤러 · Jenkins 빌드 에이전트  (taint: batch)
node5  192.168.56.15  16GB    레지스트리 · Jenkins · Argo CD · 모니터링  (taint: infra)
                              합계 RAM 40GB · 디스크 280GB
```

### 접속 정보

| 포트 | 서비스 |
|---|---|
| **30080** | 웹 앱 (Reverdi) |
| **30081** | Argo CD |
| **30300** | Grafana |
| **30500** | 레지스트리 |
| **30808** | Jenkins |

`http://192.168.56.11:<포트>` — **`.11` ~ `.15` 아무 노드로 들어와도** 됩니다.

### 🔴 비밀번호는 매번 새로 만들어집니다

파일에 적어두지 않습니다. 필요할 때 꺼내 씁니다.

```bash
vagrant ssh node0
```

```bash
# 웹 앱 admin
kubectl get secret reverdi-secret -n reverdi -o jsonpath='{.data.ADMIN_PASSWORD}' | base64 -d ; echo

# DB
kubectl get secret reverdi-db-app -n reverdi -o jsonpath='{.data.password}' | base64 -d ; echo

# Grafana
kubectl get secret grafana-admin -n monitoring -o jsonpath='{.data.admin-password}' | base64 -d ; echo

# Argo CD
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath='{.data.password}' | base64 -d ; echo

# Jenkins
kubectl exec -n infra svc/jenkins -c jenkins -- cat /run/secrets/additional/chart-admin-password ; echo
```

---

## 2. 검증 결과

> 📊 도표 2페이지 참조

### 인프라

| | 확인 방법 |
|---|---|
| ✅ 노드 6대 Ready · IP 각자 다름 | `kubectl get nodes -o wide` |
| ✅ taint 격리 3종 | node0 `CriticalAddonsOnly` · node4 `batch` · node5 `infra` |
| ✅ 레지스트리 + `registries.yaml` 전 노드 | `curl .../v2/_catalog` |

### 애플리케이션

| | |
|---|---|
| ✅ **웹 파드 3개 → node1·2·3 하나씩** | `topologySpread` + `matchLabelKeys` |
| ✅ 무중단 롤링 업데이트 | `maxUnavailable: 0` — 새 파드 Ready 후 교체 |
| ✅ 마이그레이션 Helm 훅 | alembic 14단계, 앱보다 먼저 |
| ✅ `/ready` 전부 `true` | `database` · `database_write` · `migration` |

```json
{
  "ready": true,
  "database":       { "connected": true },
  "database_write": { "connected": true },
  "migration":      { "up_to_date": true }
}
```

### 데이터베이스

| | |
|---|---|
| ✅ CloudNativePG primary 1 + replica 2 | node1·2·3 분산 (`podAntiAffinity`) |
| ✅ **페일오버 자동 승격** | 웹 응답 **200 연속 유지** · 파드 재시작 0 |
| ✅ 읽기/쓰기 분리 | 앱 로그에 `-ro` 연결 확인 |

**🔴 페일오버 시연 (발표 영상 B)**

```
09:37:40  200
09:37:41  200        ← primary 파드 삭제
   ...
09:38:08  200        끊김 0회
```

`PRIMARY` 가 `reverdi-db-1` → `reverdi-db-2` 로 자동 승격되는 동안
**웹 파드는 재시작조차 하지 않았습니다** (`RESTARTS 0`).

읽기는 replica 로 가기 때문입니다. **AWS RDS Multi-AZ 와 같은 동작**입니다.

### 배치 · 앱 기능

| | |
|---|---|
| ✅ 크롤러 node4 에서만 | Playwright + Chromium 동작 |
| ✅ `/dev/shm` tmpfs **1.0G** | 없으면 Chromium 이 죽는다 |
| ✅ 중복 수집 방지 | "다른 프로세스가 수집 중" |
| ✅ 클릭 집계 | `202 {"status":"counted"}` |
| ✅ **중복 방지** | 2번 눌러 **1건** (30분 버킷 유니크) |

```
1회차  {"status":"counted"}    + reverdi_cid 쿠키 발급
2회차  {"status":"duplicate"}  쿠키 재사용
DB     count = 1
```

### CI/CD

| | |
|---|---|
| ✅ **Jenkins 파이프라인 SUCCESS** | lint → test → build → chart lint |
| ✅ 인라인 파드 (컨테이너 5개) | python · postgres · buildah · tools · jnlp |
| ✅ Buildah 이미지 2종 | docker 데몬 없이 빌드 → push |

```
Finished: SUCCESS
빌드 성공 — 이미지 태그 a6a4091
```

### GitOps · 모니터링

| | |
|---|---|
| ✅ **Argo CD Synced / Healthy** | git 변경 감지 → 자동 동기화 |
| ✅ Prometheus + Grafana | 노드 · 파드 · 컨테이너 지표 |
| ✅ **앱 지표 수집** | ServiceMonitor → `http_requests_total` |

**구성도 4페이지가 지적한 갭이 해소됐습니다.**

> "노드·컨테이너 지표만 보이고, 요청 수·지연·에러율 같은 사용자 관점 지표는 못 봅니다"

이제 `status` 라벨까지 있어 **에러율 계산**도 됩니다.

```
sum(rate(http_requests_total[1m])) by (status)
```

---

## 3. 겪은 문제 24건

> 📋 상세는 `문제해결_기록.md` 참조

| 분류 | 건수 |
|---|:--:|
| 환경 · 도구 문제 | 7 |
| **우리 코드의 실수** | **5** |
| 이해가 부족했던 것 | 4 |

### 가장 오래 걸린 3건

| # | 문제 | 왜 어려웠나 |
|:--:|---|---|
| **6** | 노드 간 파드 통신 불가 | 증상이 **표면적 원인(방화벽)을 가리킴** |
| **9** | 파드가 한 노드에 몰림 | **설정은 맞는데** 동작이 안 됨 |
| **12** | DB 인증 실패 | **1차에서는 우연히 안 걸림** |

### 문제 6 — Flannel 이 NAT 인터페이스 선택

```bash
$ ip -d link show flannel.1
vxlan id 1 local 10.0.2.15 dev enp0s3      # ← 6대 전부 같은 IP
```

VM 에 NIC 이 두 개인데(`enp0s3` = Vagrant NAT, `enp0s8` = 사설망),
**Flannel 이 NAT 쪽을 잡았습니다.**

`--node-ip` 를 줬는데도 이렇게 된 이유 — 그건 **"쿠버네티스가 보는 노드 주소"만** 바꿉니다.
CNI 가 쓰는 인터페이스는 별개라 `--flannel-iface` 를 따로 줘야 합니다.

방화벽도 정상, 라우팅도 정상, 파드도 정상인데 **패킷만 안 갔습니다.**

### 문제 9 — `matchLabelKeys`

`topologySpreadConstraints` 를 넣고 `DoNotSchedule` 로 바꿔도 파드가 몰렸습니다.

원인은 `labelSelector` 가 롤링 업데이트 중 **옛 파드까지 세는 것**이었습니다.
스케줄러가 "저 노드는 찼다"고 판단하는데, 그 파드는 곧 사라집니다.

```yaml
matchLabelKeys:
  - pod-template-hash      # 같은 세대끼리만 계산 (k8s 1.27+)
```

**규칙 위반이 아니라 세는 대상이 틀렸던 것**입니다.

### 문제 12 — CNPG 가 Secret 을 덮어씀

`bootstrap.initdb.secret.name` 을 명시해도 **CNPG 가 초기화 시점에 새로 만듭니다.**

```
잘못  Secret 생성 → DB 생성
맞음  DB 생성 → CNPG 비번 꺼내기 → 앱 Secret 생성
```

1차 구축에서는 우연히 순서가 맞아 안 걸렸고, **재구축할 때 드러났습니다.**

### 🔴 같은 실수를 네 번 반복했습니다

`nodeSelector` 만 넣고 `tolerations` 를 빠뜨린 게 네 곳에서 나왔습니다.

| 파일 | 시점 |
|---|---|
| `infra/registry.yaml` | 1차 검토 |
| `infra/minio.yaml` | 1차 검토 |
| `helm-values/argocd.yaml` | CI/CD 도입 |
| `helm-values/kube-prometheus-stack.yaml` | 모니터링 |

**둘은 짝입니다.**

| | 역할 |
|---|---|
| `nodeSelector` | **"이 파드를 어디로 보낼지"** — 파드 입장 |
| `taint`/`toleration` | **"이 노드에 누가 들어올지"** — 노드 입장 |

taint 가 걸린 노드를 `nodeSelector` 로 지목해도, toleration 이 없으면 **거부당합니다.**

---

## 4. 담당별 작업 내역

### 👤 A — 차트 뼈대 + 웹 (8개)

| 파일 | 상태 |
|---|:--:|
| `Chart.yaml` · `values.yaml` · `_helpers.tpl` | ✅ |
| `templates/configmap.yaml` · `secret.yaml` | ✅ |
| `templates/deployment.yaml` | ✅ 🔴 **`matchLabelKeys` 추가** |
| `templates/service.yaml` · `ingress.yaml` | ✅ |

`values.yaml` 에 **환경변수 15개 추가** (백엔드 최신 반영).

### 👤 B — 배치 워크로드 + 값 파일 (7개)

| 파일 | 검토 후 수정 |
|---|---|
| `migrate-job.yaml` | 🔴 ConfigMap 을 `optional` 로 (훅 순서) |
| `crawler-cronjob.yaml` | 🔴 **`/dev/shm` 마운트 추가** (원본에 없었음) |
| `pdb.yaml` | **원본이 0바이트 빈 파일이라 새로 작성** |
| `pgdump-cronjob.yaml` | `postgres:15` → **`17-alpine`** (RDS 가 PG17) |
| `values-vagrant/aws.yaml` | 환경별 기동 가드 |

**추가로 고친 것** — `crawler.command` 가 `python -m crawler.main` 이었는데
**그런 모듈이 없습니다.** `python -m app.crawler --once` 로 고쳤습니다.

이미지 이름도 `reluxe-web`(nginx 이미지) → **`reverdi-backend`** 로 바로잡았습니다.

### 👤 C — 인프라 + CI/CD (7개 + Jenkinsfile)

| 파일 | 상태 | 검토 후 수정 |
|---|:--:|---|
| `infra/registry.yaml` | ✅ | 🔴 toleration 추가 |
| `infra/postgres-cluster.yaml` | ✅ | PG 16.4 → **17.2** |
| `infra/registries.yaml` | ✅ | |
| `infra/minio.yaml` | ⏸ | toleration 추가. 미배포 |
| `argocd/application.yaml` | ✅ | 저장소 주소 수정 |
| `helm-values/argocd.yaml` | ✅ | 🔴 **toleration 추가** · 재작성 |
| `helm-values/jenkins.yaml` | ✅ | 🔴 `agent.enabled` |
| `helm-values/kube-prometheus-stack.yaml` | ✅ | 🔴 prometheus·grafana toleration |
| `Jenkinsfile` | ✅ | 🔴 **대폭 수정** — 아래 |

**Jenkinsfile 수정 내역**

| 문제 | 왜 실패하는가 | 고침 |
|---|---|---|
| 한 컨테이너에 모든 도구 | `buildah` 이미지에 `uv`·`helm`·`git` 없음 | **컨테이너 4개 + jnlp** |
| `test` 가 `docker run` 으로 DB | k3s 는 containerd — **Docker 데몬 없음** | **사이드카 Postgres** |
| `IMAGE_TAG` 가 null | `environment` 블록은 **체크아웃 전** 평가 | `script` 블록으로 |
| `alpine/helm` 에 git 없음 | Alpine 은 git 기본 미포함 | `apk add --no-cache git` |
| `post` 가 오류를 가림 | 앞 단계 실패 시 컨테이너가 없어 정리도 실패 | `container()` 래핑 제거 |
| **`agent { label 'build' }`** | Pod Template 이 없어 무한 대기 | 🔴 **인라인 파드 정의** |

---

## 5. CI/CD 흐름

> 📊 도표 3페이지 참조

```
개발자 push → CloudeDX(git)
                   ↓
              Jenkins   lint → test → Buildah 빌드 → 레지스트리 push
                        → chart lint → reverdi(git) 에 image.tag 커밋
                   ↓
              Argo CD   git 변경 감지 → 클러스터 동기화
```

### 🔴 저장소를 둘로 나눈 이유

| 저장소 | 담는 것 | 누가 커밋 |
|---|---|---|
| **CloudeDX** | 앱 소스 · Dockerfile · Jenkinsfile | 개발자 |
| **reverdi** | Helm 차트 · infra · argocd | **Jenkins** (태그만) |

같은 저장소에 두면 **무한 루프**가 납니다.
Jenkins 가 빌드 → 태그 커밋 → 그 커밋이 Jenkins 를 다시 깨움 → 반복.

### 이렇게 나눈 이점

- Jenkins 에 **클러스터 권한이 필요 없습니다** (kubeconfig · EKS IAM 불필요)
- **git 커밋 = 배포 이력.** 지금 뭐가 떠 있는지 git 이 증명합니다
- 손으로 `kubectl edit` 해도 Argo CD 가 되돌립니다 (selfHeal)
- **롤백이 `git revert`**

> ⚠️ Argo CD 는 Helm 을 `helm template` 으로만 씁니다.
> `helm rollback` · `helm history` 가 안 됩니다.

---

## 6. 남은 작업

| 항목 | 상태 | 비고 |
|---|:--:|---|
| **Jenkins `update gitops`** | ⚠️ | `when { branch 'main' }` 로 건너뜀 |
| node-exporter node0 | ⚠️ | toleration 에 `effect` 명시 때문 |
| MinIO | ⏸ | 배포 전 |
| `pgdump` · `aggregate` CronJob | ⏸ | 진입점 미구현 |
| 웹 시연 영상 (A) | | 크롤링 데이터 있음 — 촬영 가능 |
| AWS `<S3_BUCKET_NAME>` | | 배포 전 필수 |

### `update gitops` 가 건너뛰어진 이유

```
Stage "update gitops" skipped due to when conditional
```

`Pipeline script from SCM` 으로 단일 브랜치를 받으면 `env.BRANCH_NAME` 이 채워지지 않습니다.
**Multibranch Pipeline** 으로 만들거나 조건을 완화하면 해결됩니다.

**빌드 → 이미지 push 까지는 검증됐으므로 남은 건 마지막 연결 고리 하나**입니다.

---

## 7. 팀원이 같은 환경을 만드는 법

### 🔴 전자동 스크립트가 있습니다

```powershell
vagrant plugin install vagrant-disksize
cd vagrant
vagrant up
```

**80~110분** 뒤 지금과 같은 상태가 됩니다.
마지막에 **접속 주소와 비밀번호가 한 번에 출력**됩니다.

각 스크립트에 **"이 단계가 무엇이고 왜 이렇게 하는지"** 를 적어뒀습니다.
우리가 겪은 문제도 그 자리에 있습니다.

### 필요한 것

| | |
|---|---|
| VirtualBox · Vagrant | |
| **호스트 RAM** | **48GB 이상 권장** (VM 이 40GB) |
| 디스크 여유 | 300GB |

### 자동화하지 못한 것

**GitHub 토큰**입니다. 개인 자격증명이라 스크립트에 넣을 수 없습니다.
Jenkins 자격증명 등록을 직접 하셔야 하고, 요약 화면에 3단계로 안내돼 있습니다.

> ✅ **실행 검증 완료** — 2026-09-04 `vagrant destroy` 후 처음부터 다시 돌려
> **약 90분** 만에 클러스터 · 앱 · DB HA · Argo CD · 모니터링 · Jenkins 전부 기동했습니다.
> 검증 중 막힌 두 곳(문제 17 · 18)은 스크립트에 반영했습니다.

### 다른 방법

| 방법 | 현실성 |
|---|---|
| **각자 구축** | **권장** — 재현되고, 부수고 다시 만들 수 있어야 학습이 됨 |
| 화면 공유 | 가장 확실. 보기만 가능 |
| 포트 포워딩 | 학원 정책상 막힐 수 있음 |
| VM 이미지 복사 | 30~50GB · IP·MAC 충돌 위험 |

---

## 8. AWS 로 이어지는 것

### 그대로 가는 것

- Helm 차트 `templates/` 12개 전부
- taint · toleration · nodeSelector
- `topologySpreadConstraints` + **`matchLabelKeys`** (키만 호스트명 → AZ)
- probe · PDB · rollingUpdate
- **읽기/쓰기 분리** (`-rw`/`-ro` → RDS writer/reader)
- Jenkins → git → Argo CD 흐름

### 버려지는 것

| 로컬 | AWS |
|---|---|
| Docker Registry | ECR |
| CloudNativePG | RDS Multi-AZ |
| MinIO | S3 |
| NodePort | ALB Ingress |

**매니페스트는 버려지지만, 그것으로 검증한 앱 설정과 운영 감각은 남습니다.**

### 환경 전환

```bash
helm upgrade --install reverdi charts/reverdi -f values-vagrant.yaml   # 로컬
helm upgrade --install reverdi charts/reverdi -f values-aws.yaml       # AWS
```

**값 파일 하나만 바꾸면 됩니다.**

> ⚠️ AWS 배포 전 `values-aws.yaml` 의 `<S3_BUCKET_NAME>` 을 채워야 합니다.
> `APP_ENV=production` 이라 비어 있으면 **기동을 거부**합니다. (의도된 동작)

---

## 9. 🔴 커밋하면 안 되는 것

Secret 값은 **`kubectl create` 로만** 만듭니다.

```
reverdi-db-app       DB 계정
reverdi-secret       앱 환경변수 (DATABASE_URL · SESSION_SECRET · 로그인 계정)
grafana-admin        Grafana 관리자
```

`.gitignore` 에 `helm.exe` · `out*.yaml` · `*.key` · `kubeconfig` 를 넣어뒀습니다.
`git status` 로 한 번 확인한 뒤 커밋하세요.

### ⚠️ 윈도우에서 파일을 고칠 때

**PowerShell 로 한글 파일을 일괄 치환하지 마세요.**
`Get-Content` / `Set-Content` 는 기본 인코딩이 UTF-8 이 아니라,
읽을 때 CP949 로 해석해 **한글이 깨집니다.** 이번에 파일 7개가 이렇게 망가졌습니다.

**VS Code 의 `Ctrl+Shift+H`** 를 쓰시면 인코딩을 건드리지 않습니다.

---

## 10. 정리

**① 문서에 적어둔 함정이 실제로 나타났습니다**

Rocky 박스 404, 공유 폴더, firewalld — 사전 조사한 항목이 그대로 발생했습니다.

**② 예상하지 못한 문제가 더 어려웠습니다**

Flannel 인터페이스, `matchLabelKeys`, CNPG Secret — 셋 다 문서에 없던 것이고
가장 오래 걸렸습니다. 공통점은 **증상이 표면적 원인을 가리킨다**는 것입니다.

**③ "돌아가게 하는 것"과 "제대로 고치는 것"은 다릅니다**

ConfigMap 문제를 훅으로 우회하면 당장은 돌아가지만 `helm rollback` 이 깨집니다.
부작용을 확인하고 다른 방법(`optional: true`)을 택했습니다.

**④ 재구축이 검증입니다**

이름을 바꾸며 처음부터 다시 세우자 **CNPG Secret 문제가 드러났습니다.**
한 번 성공한 것은 우연일 수 있고, **두 번째에 진짜가 보입니다.**

**⑤ 로컬 검증의 값어치**

24개 문제를 AWS 에서 만났다면 클러스터 재생성에 20분씩 걸리고 비용도 나갔을 것입니다.
로컬에서는 `vagrant destroy && vagrant up` 으로 5분이면 처음부터 다시 할 수 있었습니다.
